# План работ по итогам полевого UX-аудита — 2026-09-05

> **Статус:** в работе. Галочки и хвосты — в разделе 0.1, обновлять вместе с кодом.
> **Источник:** полевой UX-аудит 2026-09-05 (приложение LeadDrive Field 3.0.4 на Samsung S23 Ultra по adb, веб-модуль `/mtm/*` в Chrome, десктоп 1470 и планшет 834). Отчёт: https://claude.ai/code/artifact/ead7594e-cd36-4ed9-8861-fe7f22bec883
> **Репозитории:** `leaddrive-v2` (веб + API, этот репозиторий) и `leaddrive-mtm/MTMobileApp` (приложение, GitHub `rashadrahimov/leaddrive-mtm`)
> **Связанные документы:** `mtm-routes-ux-roadmap-2026-08-23.md` (RUX-xxx), `mtm-routes-phase-1-ux-contract.md`, `mtm-agent-mobile-v2-master-plan.md`, `mtm-mobile-update-spec-2026-07-11.md`

Правила исполнения — по `CLAUDE.md` обоих репозиториев: ветка → PR → адверсариальное ревью → тесты → мерж → автодеплой. Приложение собирается только в CI (пуш в `claude/**` даёт prerelease APK), проверяется на телефоне владельца по adb.

## 0. Definition of Done для каждой задачи

1. Один экран решает одну задачу. Главное действие видно без прокрутки: телефон 1080×2316, ноутбук 1470×657, планшет 834×686.
2. Никаких вводных абзацев перед элементами управления. Помощь живёт за «?» или в закрываемой подсказке первого входа.
3. Никаких значений перечислений в интерфейсе (`Active`, `AGENT`, `WEEKEND`, `PLANNED`, `Checked out`). Только словарь на трёх языках.
4. Азербайджанский — основной язык, RU и EN обязаны иметь паритет: на вебе `node scripts/check-translations.js`, в приложении паритет `src/i18n/locales/{az,en,ru}.json` и `src/i18n/mobile-resources.ts`.
5. Одна информация показывается в одном месте, одна функция доступна из одного места.
6. Задача закрывается со скриншотами «до/после» (телефон и/или веб) и тестом. Приложение: `cd MTMobileApp && npx jest`. Веб: `npm test`, базлайны `test-baseline.json` и `typecheck-baseline.json` правятся только коммитом с объяснением.
7. Имена клиентов в текстах продукта запрещены (SwissMed, SOCAR, Kapital Bank и любые другие).

## 0.1 Состояние задач

> Обновляется **в том же PR, что и код**: закрыл задачу — поставил галочку и убрал
> её хвосты. Это единственное место, где состояние плана записано; если галочки
> расходятся с репозиторием, верны репозиторий и тесты, а не эта таблица.

**Как читать.** `[x]` — задача закрыта целиком. `[ ]` с пометками «хвост» — код
влит, но приёмка выполнена не вся; хвост нужно закрыть прежде, чем ставить
галочку. `[ ]` без пометок — не начата. «Сверено с кодом 2026-09-06» значит, что
ветку `main` читали против критериев приёмки, а не доверились названию PR.

**Кто делал.** `S-plan` — сессия, писавшая этот план (worktree
`mobile-routes-app-682b45`). `S-sprint1` — сессия «MTM field UX audit sprint 1»,
завершена и заархивирована. `S-mtm` — сессия «Сессии по работе МТМ». Три сессии
шли по одному списку и дважды сделали одно и то же; прежде чем брать задачу,
проверь `gh pr list --state open` в обоих репозиториях.

**Эталон APK восстановлен, приложение снова собирается.** Гейт подписи сверял
новую сборку с релизом `v1.7.0-build137` — другой ключ, и Android откажется
ставить обновление поверх установленного. Репозитория, где этот релиз жил,
больше нет, и гейт падал на «release not found» при совершенно правильном APK.
Ослаблять его было нельзя: «прежнего релиза нет, значит всё в порядке»
обезоруживает проверку ровно там, где подменённый keystore и проехал бы.
Отпечаток закреплён константой в `build-apk.yml`
(`d6e9a6a76bb7c12e0ed81e2094f56e356e6f0d12b07ba35a2ed6598a88cecf2b`).

**Совпадение с установленной базой подтверждено 2026-09-10 фактом, а не словом
гейта.** С телефона владельца снят установленный `com.mtmobileapp` (versionCode
42, 3.0.4), блок подписи APK v2 разобран напрямую — сертификат тот же. Значит
новая сборка встаёт обновлением поверх: удалять приложение не придётся, а
несинхронизированная очередь и черновики уцелеют. Тем же способом проверена и
сама сборка 43.

**Приложение доехало до владельца.** [rashadoni/leaddrive-mtm #1](https://github.com/rashadoni/leaddrive-mtm/pull/1)
смержен, merge `5367adbd`; сборка `v3.1.0-build6` (versionCode 43) опубликована
пререлизом и отдана владельцу. В ней T7, T8, T12, B12, B14 и сторож стилей.
Это первая сборка приложения после переезда репозитория — до неё на телефоне
жила 3.0.4, и все правки в приложении, сделанные по этому плану, человек не
видел вовсе.

**Пятнадцать задач на проде.** На 2026-09-09 живёт
`e0045bf71e42d9e34e5417c38c86c981e3234d04`, `/api/v1/ping` → 200, публичный
`build-info` и `.deploy-sha` на хосте сходятся. Доставлены A2, T9, T10, T16,
C10, C11, C14, C7, C12, C6, C8, C5, C15 и хвосты C2.

**Доставка ≠ приёмка.** На проде лежит код и зелёные тесты. Часть из них —
контрактные по исходнику: они грепают файл и доказывают, что строка написана,
а не что человек увидел нужное. Функциональной проверки на экране и на телефоне
не проходила ни одна из пятнадцати; это эпик D, и он открыт. Не писать «задача
сделана», если её видел только тест.

**Зачем их собрали в одну ветку.** Пятнадцать PR прошли один комплект гейтов
вместо пятнадцати, и собранная ветка нашла три недоделки, невидимые
поодиночке: один PR убрал виджет по решению владельца и не догнал два
контрактных теста, которые его требовали; другой переименовал ключ во всех трёх
локалях и не догнал тип-union; третий унёс ключ черновика в отдельный модуль, а
тест продолжал искать литерал в компоненте. **Каждый был зелёным сам по себе.**
Пересечение видно только там, где ветки встречаются, — на `main` было бы поздно.
Приём стоит повторять, когда очередь снова накопится.

**Репозиторий переехал дважды, и адрес в этом абзаце — не вечная истина.**
Сначала на Azure, затем 2026-09-09 на публичный
`github.com/rashadoni/leaddrive-v2`, откуда прод и выкатывается. Старый
`rashadrahimov/leaddrive-v2` мёртв: Actions выключены, раннеры Contabo сняты,
прогоны висят `queued` или падают `startup_failure` — **это ожидаемо, не чинить**.
У нового `main` нет общей истории со старым: rebase и cherry-pick через границу
не проходят, незавершённое переносится патчем
(`git diff <база>...<ветка>` → новая ветка от `origin/main` → `git apply --3way`). Ссылки на PR в записях задач ниже ведут в старый репозиторий и
уже не открываются: это история о том, чем задача была закрыта, а не рабочие
адреса. Не переписывать их на новые номера — там их нет.

Правила CI живут в `AGENTS.md` и `CLAUDE.md` нового репозитория и там же
проверяются: `runs-on: ubuntu-24.04` везде, `macos-*` запрещён скриптом
`scripts/ci/check-github-runner-policy.mjs`, метки `leaddrive-ci` /
`leaddrive-typecheck` / `leaddrive-ci-light` мертвы, обязательные проверки —
`agent-review` и `runner-policy`, `pull_request_target` не использовать.

**Прод проверять фактом, а не цветом прогона** — деплой умеет краснеть уже
после успешной выкатки. Два независимых источника:
`curl https://app.leaddrivecrm.org/api/v1/public/build-info` и
`ssh root@13.140.132.245 'cat /opt/leaddrive-v2/.next/standalone/.deploy-sha'`
(файла `/opt/leaddrive-v2/.deploy-sha` не существует).

**Осталось в приложении.** `leaddrive-mtm`: T7 (баннер офлайна), T8 (центр
синхронизации), T12 (ключ карты), B12 (контуры синхронизации) собраны в одну
ветку и не смержены. Их CI поймала то, чего не увидели 647 зелёных тестов:
`SyncStatusChip.tsx` звал `fieldTheme` без импорта, а `StyleSheet.create`
выполняется при импорте модуля — центр синхронизации не загрузился бы вовсе.
Тестов, которые загружают сам компонент, не было: логику вынесли в чистые
модули и покрыли их, а файл, который их собирает, не импортировал никто.
Починено, добавлен тест, который просто импортирует компонент.

**Что из очереди 4 проверено на «нужен ли экран».** Не всё, что выглядит
вёрсткой, ею является, и обратное тоже верно:

- **B12 сделан** ([mtm #87](https://github.com/rashadrahimov/leaddrive-mtm/pull/87)) —
  оказался про строки и фильтр контуров, а не про компоновку.
- **B18 наполовину уже верен на `main`.** «Описание без глифа» выполнено:
  в приложении 121 `accessibilityLabel`, и ни одна не собирается из массива с
  иконкой — единственный массив (`WeekScreen.tsx`, строка карточки задачи)
  состоит только из текста. Симптом «, Начать день» из аудита не
  воспроизводится; вычистил спринт 1 вместе с B13/B15. Открытым остаётся
  второй пункт — подпись вкладок 12 px вместо нынешних 10
  (`AppNavigatorAndroidV2.tsx`, `tabLabel.fontSize`), и это уже пиксели:
  проверять на устройстве.
- **B5 наполовину тоже уже верен.** Условие пустого дня в `WeekScreen.tsx`
  (`DayAgenda`, `agendaEmpty`) требует нуля по четырём величинам сразу —
  `visitsTotal`, `tasksTotal`, `visits.length`, `tasks.length`, — то есть
  строже, чем просил аудит. «На этот день планов нет» поверх существующих
  задач не показывается. Открытым остаётся экранное: чип статуса маршрута и
  переход из карточки в экран дня.
- **B6, B7, B8, B9, B10, B11, B17, B19, B20, B21** — компоновка и жесты.

Вывод, который стоит держать в голове дальше: в этом плане **«не начато» и
«не сделано» — разные вещи**. Два пункта из двух, которые выглядели
логическими и проверяемыми, оказались уже выполненными на `main` попутной
работой спринта 1. Прежде чем брать задачу из очереди 4, прочитай код по
адресу из «Где»: часть претензий аудита от 2026-09-05 уже неактуальна.

**Две задачи бэклога проверены и описаны неверно — не делать как написано.**

- **T11** просит формы множественного числа трём ключам. Две из них
  (`visit.visitsCount`, `week.tasksTemplate`) не отрисовываются нигде, третья
  (`task.pendingSyncTemplate`) упомянута только в тесте на наличие `{{n}}`. Экран
  задач при этом носит свой трёхъязычный словарь прямо в компоненте
  (`MTMobileApp/src/screens/tasks/TasksScreen.tsx`, объект `copy`). Настоящая
  работа — увести экран на i18n; склонять мёртвые строки смысла нет. То же
  записано хвостом B14.
- **T15** в приёмке требует, чтобы «Top 3» брался из исходного рейтинга, а не из
  отсортированного пользователем списка. На текущей странице
  (`mtm/leaderboard/page.tsx`) сортировки нет вообще, `rankings` приходят с
  сервера и `slice(0, 3)` честен. Это не скрытый баг, а требование к будущей
  таблице: задача остаётся переделкой экрана, её нельзя закрыть логической
  правкой.

**Прод снова принимает выкладки — с вечера 2026-09-07.** До этого он два дня жил
на сборке `8fbd00c` от 2026-09-05, сделанной ещё до плана, и всё влитое копилось в
`main`. Проверено 2026-09-08: четыре успешных прогона `deploy.yml` подряд, последний
`34191648499` на `82fae5fe5` (мерж #1129), `/api/v1/ping` отвечает `{"ok":true}`.
То есть спринт 1 доехал до людей.

Оба прежних блокера сняты. Требование независимого человека-ревьюера у окружения
`production` убрано 2026-09-06 вместе с переходом на четыре слоя доставки
([#1105](https://github.com/rashadrahimov/leaddrive-v2/pull/1105),
`docs/DELIVERY-ARCHITECTURE.md`): на тарифе GitHub Pro для приватного репозитория
такой настройки не существует, поэтому она не защищала, а делала выкатку
невозможной. Бэкапные гейты разобраны 2026-09-07 (#1111, #1112 и правки на хосте);
настоящим блокером там оказалась роль `leaddrive_migrator`, потерявшая `CREATE` на
схему `public`, — гейты бэкапа только прятали её собой.

**Но красный `deploy.yml` больше не значит «не доехало».** Прогон умеет краснеть
уже ПОСЛЕ успешной подмены версии — на `FATAL: offline restore marker must be a
regular file`; этот маркер создаётся церемонией с приватным ключом и офлайн-хостом,
то есть владельцем. Прежде чем писать «на проде этого нет», смотреть надо не на
цвет прогона, а на шаг, где он упал, и на сам прод.

**Что брать следующим** — `docs/mtm-field-ux-atomic-backlog.md`: остаток плана,
разобранный на атомарные задачи по правилу «одна задача — один PR — одна
приёмка», в порядке от «агент теряет работу» к «внешний вид». Состояния там
нет намеренно: оно живёт здесь, в разделе 0.1, чтобы два места не разъехались.

Развёрнутая картина со ссылками на PR и найденными хвостами:
https://claude.ai/code/artifact/24018303-754d-41bc-aeb4-3af0ea5b0bf1

### Эпик A — Сервер и API · закрыто 7 из 7

- [x] **A1** Координаты «нет данных» вместо 0,0 — P0 · [leaddrive-v2 #1083](https://github.com/rashadrahimov/leaddrive-v2/pull/1083), [#1107](https://github.com/rashadrahimov/leaddrive-v2/pull/1107) · S-sprint1 + S-plan · сверено с кодом 2026-09-06
  - Хвост закрыт: `week` и `visits/[id]/workspace` прогоняют клиента через `withNormalizedCoordinates`, а коридор отклонения в `location` больше не берёт (0,0) вершиной — иначе агент у двери читался как «800 м от маршрута» и получал пуш. `kpi` и `workday` перепроверены: их широта и долгота — это GPS-точки агента и события смены, координат клиента там нет вовсе.
- [x] **A2** Один источник «доступных точек» для агента — P0 · [leaddrive-v2 #1094](https://github.com/rashadrahimov/leaddrive-v2/pull/1094), #1097 (слит через интеграционную ветку) · S-plan + S-mtm · сверено с кодом 2026-09-09
  - Хвост закрыт: `validateMtmMobileRouteTargetEligibility` больше не требует назначения — проверка идёт по общей области (`eligibleCustomerIds`), в которой ветка назначений лишь одна из двух, и в коде это помечено как заведомый супермножество прежней. Пикер и валидатор с тех пор согласны: точка, предложенная планировщиком, сохраняется.
  - Хвост закрыт наполовину и переформулирован дважды, потому что оба раза указывал не туда. Настоящий разрыв был не в кодах причин: `/api/v2/.../planning-targets` — единственный эндпоинт целей, который приложение вызывает, — фильтровал организации **только по назначениям**, а валидатор записи и `/api/v1/.../routes/targets` спрашивали общую область (назначение ИЛИ действующий маршрут). Планировщик показывал агенту строго меньше, чем сервер готов принять: клиент, доступный через маршрут, сохранялся, но в списке не появлялся. Это зеркало исходной ошибки A2. Теперь все три пути зовут `eligibleFieldCustomerWhere`. Врачи намеренно остались по назначениям — расширение дало бы запись, которой интерфейс не показывал; так написано и в валидаторе.
  - Хвост закрыт для организаций: `/api/v2/.../planning-targets` объясняет пустой список — дата, выходной, решение по календарю, отсутствие доступных точек. Молчит намеренно там, где объяснить честно нечем: если пусто из-за строки поиска или фильтра типа, это не «нет назначений» и не «территория», а слова для «ваш фильтр ничего не нашёл» в общем словаре нет. Неверная причина отправляет агента к руководителю из-за опечатки; отсутствие причины позволяет экрану сказать простое. Расчёт живёт внутри ветки пустого ответа — обычный путь этот список открывают десятки раз в день.
  - Хвост закрыт: врачи тоже объясняют пустой список. Тонкость в том, что их ищут в две фазы — сначала прямые назначения на контакт, потом назначения на клиента его рабочего места, — и пустая первая фаза ещё не конец списка: `nextPage` указывает на вторую. Причина считается только когда обе исчерпаны, иначе агент прочитал бы «нет назначений» за миг до того, как вторая фаза вернёт людей. «Есть ли вообще доступные» считается по обеим формам назначения: одна арма сказала бы «нет назначений» тому, у кого есть другая.
- [x] **A3** Незавершённые маршруты — P0 · [leaddrive-v2 #1092](https://github.com/rashadrahimov/leaddrive-v2/pull/1092) · S-plan · сверено с кодом 2026-09-06
- [x] **A4** Локализованные оповещения — P1 · [leaddrive-v2 #1137](https://github.com/rashadrahimov/leaddrive-v2/pull/1137) · S-plan · сверено с кодом 2026-09-08
  - Серверная половина закрыта: генераторы пишут `messageKey` и `messageParams` в `metadata` (`src/lib/mtm/alert-messages.ts`), веб-список собирает текст на языке менеджера, старые строки показываются как есть с пометкой «до перевода». Ключи есть на en/ru/az, тест сторожит и наличие строки, и то, что она тратит ровно те параметры, которые шлёт генератор.
  - Обе половины влиты: сервер [#1137](https://github.com/rashadrahimov/leaddrive-v2/pull/1137) merge `3d6789b5`, приложение [leaddrive-mtm #79](https://github.com/rashadrahimov/leaddrive-mtm/pull/79) merge `a040a69f`.
  - Окно «Активных» закрыто там же: нерешённые старше 14 дней в список не попадают.
- [x] **A5** Словарь статусов — P1 · [leaddrive-v2 #1088](https://github.com/rashadrahimov/leaddrive-v2/pull/1088) · S-sprint1 · сверено с кодом 2026-09-06
  - Хвост закрыт (T9): группа `photo` добавлена в `MTM_STATUS_GROUPS`, страница фото переведена целиком — тост, бейдж и подпись в сравнении; английские тексты ошибок тоже переехали в словарь. Тест сверяет группу с `MtmPhotoStatus` в схеме и стережёт страницу от возврата сырых значений.
  - Хвост закрыт (T10): роль и статус агента идут через словарь `mtmStatus` (новая группа `agentStatus`). Разом ушли две ошибки: незнакомое значение больше не печатается сырым, и администратор перестал показываться менеджером — ADMIN и MANAGER вели на одну подпись. Тест сверяет обе группы с `MtmAgentRole` и `MtmAgentStatus` в схеме.
- [x] **A6** Контракт ошибок чек-ина — P0 · [leaddrive-v2 #1084](https://github.com/rashadrahimov/leaddrive-v2/pull/1084) · S-sprint1 · сверено с кодом 2026-09-06
- [x] **A7** Перерыв в рабочем дне — P1 · S-plan · сверено с кодом 2026-09-08
  - Серверная половина закрыта: `src/lib/mtm/workday-pauses.ts` восстанавливает отрезки перерывов из событий смены; `mobile/location` POST отказывает точке, СНЯТОЙ во время перерыва (`MTM_LOCATION_WORKDAY_PAUSED`), но принимает снятую до него и доставленную из офлайн-очереди позже; история GPS отдаёт `pauses`. Тесты: 13 на разбор отрезков, 2 на приём точки, 2 на то, что завершённый день нельзя ни перезапустить, ни поставить на паузу.
  - Переходы PAUSE/RESUME/FINISH и запрет менять завершённый день уже были в `applyMtmWorkdayEvent` — их не переписывал, а покрыл тестами.
  - Клиентская половина закрыта: [leaddrive-mtm #81](https://github.com/rashadrahimov/leaddrive-mtm/pull/81), merge `cc4733da`.
  - Хвост закрыт наполовину и намеренно: карточка агента на вебе теперь показывает состояние рабочего дня — «Перерыв с 13:05», «День завершён в 18:24», «День не начат», — и это важнее, чем кажется. Сервер по A7 не принимает GPS-точки, снятые на перерыве, поэтому `lastSeenAt` у ушедшего на обед замирает, и он гас так же, как агент с севшим телефоном; руководитель звонил второму. Состояние дня теперь побеждает точку «давно не видели»: перерыв — это факт, а «не видели 40 минут» — вывод из отсутствия факта. Присутствие деградирует, а не роняет список: упавший запрос стоит одной подписи, а не всего экрана.
  - Хвост закрыт: отрезки дошли до веба. На карточке агента стоит строка «Перерывов: 2 · 45 мин» — «на перерыве» отвечает на «что происходит сейчас», а руководителю в конце дня нужно другое: два перерыва по двадцать минут и один на два часа — это одно слово и разные дни. Незакрытый перерыв считается до текущего момента, иначе стоящий на перерыве читал бы «1 перерыв, 0 минут». Отрезок, у которого конец раньше начала (такое пишет инструмент коррекции), не считается вовсе — иначе он молча укоротил бы итог. Один запрос на рабочие дни страницы, не на строку.

### Эпик B — Приложение · закрыто 21 из 22, ещё 1 влита и ждёт приёмки

> **Приёмка на устройстве 2026-09-13** (SM-S918B, Android 16, трёхкнопочная навигация, 1080×2316, релиз `v3.1.0-build56` = `main` `0a23be79`) закрыла B6, B8, B9, B10, B11, B17, B18, B21. Открыты B5, B7, B19, B20 — у каждой записан замер или точная причина. **14 сентября** закрыты B5, B7 и B16; открыт B20 (экран входа — пароль владельца); B19 закрыт 14 сентября в ландшафте, B4 — там же, по переключению сети владельцем.
>
> B5, B6, B7, B8, B9, B10, B11, B17, B18, B19, B20, B21 влиты в `main` и лежат в сборке
> приложения; галочки у них нет, потому что приёмка каждой — это измерение на
> телефоне, а не строка в исходнике. Не переделывать их заново: смотреть на код
> и на merge SHA.

- [x] **B1** Внеплановый визит: любой исход виден — P0 · [leaddrive-mtm #75](https://github.com/rashadrahimov/leaddrive-mtm/pull/75), [#74](https://github.com/rashadrahimov/leaddrive-mtm/pull/74), [#77](https://github.com/rashadrahimov/leaddrive-mtm/pull/77) · S-plan + S-sprint1 · сверено с кодом 2026-09-06
  - Хвост закрыт: [leaddrive-mtm #80](https://github.com/rashadrahimov/leaddrive-mtm/pull/80) (задача T1), merge `ace51374`. Повтор проходит через `retryOutboxConflict`, строка возвращается в очередь с новым id, и экран ждёт именно его; `flushOutbox` берёт только `pending`, поэтому старый путь не отправлял ничего. Если повторить не удалось, а строка всё ещё в `conflict`, агенту говорят прямо, что отметка не отправлена, — визит больше не выдаётся за улаженный.
- [x] **B2** Расстояние до точки — P0 · [leaddrive-mtm #75](https://github.com/rashadrahimov/leaddrive-mtm/pull/75), [#74](https://github.com/rashadrahimov/leaddrive-mtm/pull/74), [#77](https://github.com/rashadrahimov/leaddrive-mtm/pull/77) · S-plan + S-sprint1 · сверено с кодом 2026-09-06
- [x] **B3** Состояние «день завершён» — P0 · [leaddrive-mtm #74](https://github.com/rashadrahimov/leaddrive-mtm/pull/74) · S-sprint1 · сверено с кодом 2026-09-06
  - Хвост закрыт: [leaddrive-mtm #81](https://github.com/rashadrahimov/leaddrive-mtm/pull/81) (задачи T2, T3), merge `cc4733da`. Кнопка «Перерыв»/«Продолжить» ставит PAUSE и RESUME из приложения; доступна только подтверждённой смене, иначе пауза встала бы за событием START, которое ещё могут отклонить.
  - Хвост закрыт там же: `pausedAt` доезжает и с сервера, и от собственного нажатия, карточка говорит «Перерыв с 13:05».
  - Хвост закрыт: [leaddrive-mtm #82](https://github.com/rashadrahimov/leaddrive-mtm/pull/82) (задача T4), merge `d030afb6`. Правило живёт в `visit-checkin-model.ts` и стоит первым — раньше координат и раньше GPS, иначе агенту называлась бы не та причина.
  - Хвост закрыт сервером: [leaddrive-v2 #1140](https://github.com/rashadrahimov/leaddrive-v2/pull/1140) (A7), merge `a140e3dc`, на проде с 2026-09-08. `mobile/location` отказывает точке кодом `MTM_LOCATION_WORKDAY_PAUSED` по времени съёмки, а не по текущему статусу смены: снятая до перерыва и доставленная позже принимается, снятая на обеде — нет.
- [x] **B4** Офлайн — P0 · [leaddrive-mtm #74](https://github.com/rashadrahimov/leaddrive-mtm/pull/74) · S-sprint1 · сверено с кодом 2026-09-06 · принято на телефоне 2026-09-14
  - Хвост закрыт: [rashadoni/leaddrive-mtm #1](https://github.com/rashadoni/leaddrive-mtm/pull/1), merge `5367adbd`. Центр синхронизации не предлагает несбыточного — кнопка неактивна, когда нажимать бессмысленно, и рядом строка почему. Нет доступа и нет сети разведены намеренно: первое агент не изменит, второе пройдёт само.
  - **хвост:** Приёмка на телефоне (авиарежим → чип за 5 с, возврат сети → «Синхронизировано») по коду подтверждается лишь как путь событий; фактический тайминг и сценарий возврата на устройстве не…
  - **Принято на телефоне 2026-09-14** (релиз `v3.1.0-build81`, `main` `eb0fe36d`). Владелец выключил сеть; наблюдение раз в ~1,5 с по `dumpsys connectivity` и дереву экрана «Bu gün»: t=136 с чип «○ Oflayn», t=144 с система подтверждает отсутствие сети — чип остаётся «Oflayn»; t=163 с сеть вернулась — в той же пробе «✓ Sinxronizasiya edilib».
  - **Ловушка приёмки, записать:** на Samsung авиарежим не выключает Wi-Fi, если тот был включён — первые две попытки телефон оставался в сети (значок Wi-Fi рядом с самолётом), и чип честно показывал «Sinxronizasiya edilib». Для проверки офлайна выключать и Wi-Fi.
  - Хвост закрыт: [leaddrive-mtm #83](https://github.com/rashadrahimov/leaddrive-mtm/pull/83) (задача T6), merge `00f5f61a`. Возврат сети снимает устаревшую фазу `offline` в сторе; `syncing` и `error` не гасятся, неизвестная связь офлайн не снимает.
  - Хвост закрыт: [rashadoni/leaddrive-mtm #1](https://github.com/rashadoni/leaddrive-mtm/pull/1), merge `5367adbd`. Причина берётся из двух фактов — упал запрос и что говорит NetInfo, — и так на **восьми** экранах, а не на одном. Шаблон оказался шире хвоста: «сохранённые данные» приравнивались к офлайну в календаре, задачах, обзоре организаций, карточке контакта, визитах и рабочем пространстве визита. Тест перечисляет все восемь поимённо, чтобы девятый нельзя было добавить молча.
- [x] **B5** Карточка дня в календаре — P0 · [rashadoni/leaddrive-mtm #10](https://github.com/rashadoni/leaddrive-mtm/pull/10) merge `3abe5cdb` · S-plan · **приёмка на устройстве 2026-09-10** (SM-S918B, 1080×2316, сборка 3.0.4/42) · принято на телефоне 2026-09-14
  - Код влит. Статус маршрута доезжает до карточки: `mapDay` его выбрасывал при разборе `/mobile/week`. Слов писать не пришлось — словарь A5 держит группу `route` на трёх языках. Когда у дня несколько маршрутов с разными статусами, чипа нет вовсе: назвать день «Запланирован», когда половина пройдена, хуже, чем промолчать.
  - У сегодняшней карточки с маршрутом появилась кнопка «Открыть маршрут». Она переключает вкладку, а не открывает экран стеком: «Маршрут» — соседняя вкладка, и в `RootStackParamList` имени для неё нет.
  - Компоновку недели закрыл B6; пустое состояние было верно и до этого.
  - Из пяти требований выполнено одно. Это тот случай, ради которого эпик D и существует: по коду задача выглядит начатой, на экране — нет.
  - ✓ **Кнопка «Bu gün» выбирает сегодня.** Нажатие прокручивает список к 10 сентября, день помечен «Bu gün».
  - ✗ **Ленты семи дней нет.** Вместо неё семь полноразмерных карточек в столбик, по **754 px** каждая при экране 2316: на экран не помещается и трёх. Неделя без плана — это около 5300 px прокрутки, чтобы убедиться, что делать нечего.
  - ✗ **Раскрыта не одна карточка, а все.** У 10 и 11 сентября структура одинаковая до символа: заголовок, число, «0 nöqtə», три метрики и пустое состояние «Bu gün üçün plan yoxdur» с подсказкой. Выбранный день ничем не отличается от остальных.
  - ✗ **Открывается не на сегодняшнем дне.** Список стартует с 7 сентября — понедельника недели, — при том что сегодня 10-е.
  - ✗ **Сводка недели не в одну строку:** три метрики блоками, `y=599..1195`, около 600 px до того, как начнётся первый день.
  - Как проверялось: `uiautomator dump` до и после нажатия «Bu gün», координаты и высоты взяты из самого дерева, а не на глаз.
  - **Повторная приёмка 2026-09-13 на build56.** ✓ лента из семи дней и один раскрытый день; ✓ открывается на сегодняшнем дне (13-е выбрано); ✓ сводка недели в одну строку (`y=714..816`); ✓ статус маршрута на карточке — «Planlaşdırılıb» у 14 сентября с «2 nöqtə» и точкой под днём в ленте.
  - **Не проверено: «Marşrutu aç».** Кнопка по коду рисуется только у сегодняшней карточки с маршрутом (`day.isToday && day.routeCount > 0`), а 13 сентября — воскресенье без маршрута. Проверяется в первый день, когда у агента есть маршрут на сегодня.
  - **Дефект, найден на экране:** у дня с запланированным маршрутом та же карточка пишет «Bu gün üçün plan yoxdur». Пустое состояние (`DayAgenda`) смотрит только на визиты и задачи и не знает про `plannedStops`. По-азербайджански фраза к тому же говорит «сегодня» на карточке любого дня; RU «На этот день планов нет» и EN сформулированы верно.
  - **Дефект исправлен и принят на телефоне 2026-09-13:** [rashadoni/leaddrive-mtm #20](https://github.com/rashadoni/leaddrive-mtm/pull/20), merge `8639b66c`. При `plannedStops > 0` пустое состояние не рисуется; AZ-фраза стала «Bu tarix üçün plan yoxdur». На телефоне: у 14 сентября статус, «2 nöqtə» и метрики без строки «plan yoxdur»; у 15-го без плана — «Bu tarix üçün plan yoxdur». В релизе `v3.1.0-build67` (`main` `caf2f9a6`).
  - **Принято на телефоне 2026-09-14** (релиз с `main` `caf2f9a6`+): карточка сегодняшнего дня — «14 Sentyabr … Bu gün», «Planlaşdırılıb», «2 nöqtə» и кнопка «Marşrutu aç»; нажатие переключает на вкладку «Marşrut» с «Bugünkü marşrut».
- [x] **B6** Компоновка календаря — P1 · [rashadoni/leaddrive-mtm #5](https://github.com/rashadoni/leaddrive-mtm/pull/5) merge `569497b8` · S-plan · принято на телефоне 2026-09-13
  - Код влит. Приёмка B5 на устройстве измерила то, что аудит описал словами: семь полноразмерных карточек по 754 px при экране 2316, три не помещаются, неделя без плана — около 5300 px прокрутки. Все семь были раскрыты одинаково, поэтому выбранный день не отличался от остальных, а список открывался с понедельника при среде на календаре.
  - Правильная форма в файле уже была — для планшета: список дней и один раскрытый день. Телефон получил ту же мысль в своих пропорциях: лента из семи плиток и одна карточка под ней. Плитка несёт день недели, число и точку, если на этот день что-то есть; семь пар счётчиков в строку — таблица, которую никто не читает. Для скринридера плитка называет дату целиком, иначе она читалась бы как «10».
  - «Bu gün» больше не ищет карточку по offset: искать нечего. Сводка недели встала в строку — три метрики столбиком занимали около 600 px до первого дня.
  - **Принято на телефоне 2026-09-13** (build56): лента дней на `y=1208..1388`, раскрытая карточка дня заканчивается около `y=1725` при области экрана до 1981 — неделя и день в один экран, без прокрутки.
- [x] **B7** «Сегодня» в один экран — P1 · [rashadoni/leaddrive-mtm #4](https://github.com/rashadoni/leaddrive-mtm/pull/4) merge `ae5f2f0f` · S-plan · принято на телефоне 2026-09-14
  - Код влит: ниже сгиба лежали ровно два блока, и оба повторяли соседей. «Быстрые действия» — те же четыре вкладки таб-бара двумя сантиметрами ниже; «Коротко за сегодня» — та же пара «сделано/всего», что уже стоит в сводке недели на «Календаре», и утром обе половины показывают нули. У карточки «Следующий шаг» снят `minHeight: 310`: на пустом дне это иллюстрация, держащая сгиб открытым ради четырёх строк.
  - Одну величину счётчики держали в одиночку — просроченные задачи. В недельной сводке её нет, а карточка следующего шага при живом маршруте молчит о ней вовсе (`nextKind === "route"` перебивает `tasks`). Удалить секцию целиком значило бы, что агент узнаёт о просрочке, когда она случится; осталась одной строкой со стрелкой в «Задачи».
  - **хвост:** приёмка на телефоне (эпик D): без маршрута экран не прокручивается, с маршрутом следующая точка видна над сгибом. Это измерение на устройстве, тестом оно не берётся — тест держит лишь то, что повторы не вернутся.
  - **Замер 2026-09-13 (build56): без маршрута экран прокручивается — не принято** (запись до правки ниже). При идущем рабочем дне кнопка «Təqvimi aç» стоит на `y=1944..2079`, а область экрана кончается на 1981; карточка «Növbəti addım» уходит под сгиб. Вклад в это даёт и лишний отступ: `useTabBarPadding` резервирует под списком высоту панели вкладок (63 + 8 + 12 dp ≈ 233 px), хотя панель не накрывает экраны — навигатор ставит её под ними. Вторая половина («с маршрутом следующая точка над сгибом») не проверялась: маршрута на сегодня нет.
  - **Исправлено и принято на телефоне 2026-09-13:** [rashadoni/leaddrive-mtm #21](https://github.com/rashadoni/leaddrive-mtm/pull/21), merge `caf2f9a6`, релиз `v3.1.0-build67`. «Fasilə» и «Günü bitir» в одну строку (на телефоне без значков — иначе русская «Завершить день» упиралась в скругление); `useTabBarPadding` в обоих файлах хука больше не резервирует высоту панели вкладок; убрана надпись «Bu gün üçün köməkçi» над приветствием. Замеры по шагам: 2003 px содержимого после первых двух правок (прокрутка 22 px) → **1941 px при области 1981** — прокрутки нет. RU: «Завершить день» 599–937 в кнопке 551–987.
  - **Вторая половина принята на телефоне 2026-09-14:** с маршрутом «Marşrutun növbəti nöqtəsi: ADV-DEMO Store 1» на `y=1298..1391`, «Qalan nöqtə: 2», «Marşruta başla» на 1547–1682; содержимое 1786 px при области 1981 — прокрутки нет.
  - Найдено, не хвост B7: рабочий день, начатый 13-го в 20:57, 14-го показывается как «İş günü 20:57-da başlayıb» — без даты, будто сегодня.
- [x] **B8** Планировщик своего маршрута — P1 · [rashadoni/leaddrive-mtm #6](https://github.com/rashadoni/leaddrive-mtm/pull/6) merge `9f7afc83` · S-plan · принято на телефоне 2026-09-13
  - Четыре пункта из шести влиты. Главный — причина пустого списка. Сервер по A2 называет одну из пяти (`date`, `weekend`, `policy`, `assignment`, `territory`), и в `field-eligibility.ts` прямо написано «the app adopts the new field in epic B»; приложение выбрасывало это поле на транспорте — `loadTargets` возвращал только `{targets, nextPage}`. Теперь причина доезжает до карточки. Незнакомое значение считается отсутствием причины: выдумывать её — это ровно то, как была написана прежняя подсказка «назначьте базу или территорию в веб-версии», то есть совет открыть экран, которого у агента нет.
  - Шаг 1 в self-режиме больше не спрашивает «Кто поедет и в какой день»: заголовок «Ваш маршрут: выберите дату», панель сотрудника убрана — это была одна запертая строка с собственным именем агента под вопросом без вариантов. У менеджера выбор нетронут.
  - Дата на шаге 2 называется один раз, в заголовке редактируемого дня. Чип с короткой датой остался в недельном плане, где говорит, какой из семи дней фильтруется. Кнопка «?» переехала в шапку: под степпером первым шёл элемент управления про чтение.
  - Хвост закрыт: [rashadoni/leaddrive-mtm #17](https://github.com/rashadoni/leaddrive-mtm/pull/17), merge `d366a9da`. Стрелки листают месяц, под ними сетка с понедельника: выходные затенены, прошедшие дни не предлагаются вовсе, «Сегодня» ведёт на ближайший рабочий день. Аудит попал на субботу просто потому, что открыл планировщик в субботу.
  - Арифметика вынесена в `services/planning-month.ts` и покрыта тестом без экрана — там такое и ломается: концы месяцев (31 марта минус месяц это не 31 февраля), сдвиг на понедельник (у воскресенья шесть пустых клеток, а не ноль) и часовой пояс. Наружу идут ключи `YYYY-MM-DD`, не `Date`: молча уехавшее на пояс значение — тот класс ошибок, который продукт уже дважды оплатил в A1 и C2.
  - Праздники намеренно не угадываются на клиенте: рабочий календарь тенанта проверяет сервер, причину пустого дня объясняет A2. Вторая тихая копия правды хуже отсутствия подсказки.
  - **Второй хвост разобран и снят как неверный.** Скрывать вход по eligibility нельзя: доступность считается **на дату**, а вход не привязан к дате. Агент, у которого сегодня пусто, а в понедельник есть точки, потерял бы сам планировщик — это хуже, чем пройти два шага и прочитать причину. Плюс проверка требовала бы запроса кандидатов на каждый рендер экрана, который агент открывает десятки раз в день; в A2 я по этой же причине держал расчёт причины внутри ветки пустого ответа.
  - Половина по политике и так работает: вход показывается только при `policies.canPlanOwnRoutes` и роли AGENT. Остальное закрывает не сокрытие, а честная причина на пустом списке — она влита в этом же B8.
  - ~~**T5 закрыт наполовину, и вторая половина — не работа, а решение.**~~ ~~Бэклог просит показывать причину пустого списка «в планировщике и на „Визитах“». Планировщик закрыт здесь. «Визиты» берут список через `api.getCustomers()` → `/api/v1/mtm/customers` — это общий CRM-эндпоинт клиентов, а не полевой: он ничего не знает про `eligibleFieldCustomerWhere` и не отдаёт `eligibility`. Приделать к нему полевую причину значит либо протащить полевую логику в общий список, либо перевести «Визиты» на полевой эндпоинт. Второе честнее, но это смена источника данных у экрана, а не хвост. Не начинал: сначала решение, потом код.~~
  - **Пересмотрено 2026-09-13: решения не нужно, вторая половина T5 уже на экране.** Посылка «на „Визитах“ пустой список молчит о причине» не подтвердилась по коду. У `/api/v1/mtm/customers` для агента ровно один фильтр, `customerScopeForActor` (`src/lib/mtm/field-scope.ts`): клиент виден, если у агента есть действующее на дату назначение на него (`activeFieldAssignmentWindow`) или клиент стоит в недавнем маршруте этого агента. Выходного, политики, территории и даты как причины у этого списка нет — они есть у полевой доступности планировщика, и там причины уже показываются (B8).
  - Значит, пустой список «Визитов» имеет одну причину — назначения, и экран её называет на трёх языках (`VisitScreen.tsx`, `noClientsTitle`/`noClientsBody`): «Нет доступных клиентов. Обновите экран. Если список всё равно пуст, попросите менеджера проверить назначения». Пустой поиск при этом говорит своё (`noSearchTitle`), а не про назначения. Перевод «Визитов» на полевой эндпоинт ради пяти кодов причин добавил бы четыре кода, которые этот список выдать не может.
  - **Принято на телефоне 2026-09-13** (под Anar Mammadov, с разрешения владельца «создай маршрут»). Понедельник 14 сентября выбран в месячной сетке — прошедшие дни 5–12 `enabled=false`; добавлены ADV-DEMO Store 1 и Store 2 на 09:00 и 09:30; «Qaralamaları saxla» → «1 qaralama saxlanıldı.»; режим «Dərc et» → «1 marşrut dərc edildi.», чип «Dərc edilib». Маршрут остался на проде как демо-данные для B5/B9.
  - Найдено по дороге, не хвосты B8: сообщение об успехе стоит вверху экрана, а у нажатой кнопки внизу в этот момент видно только «Saxlanacaq qaralama yoxdur.», что читается как ошибка; главная кнопка во множественном числе («Qaralamaları saxla») при одном маршруте; заголовок «Öz marşrutunuzu yaradın» переносится в три строки; сводка «0 müştəri seçilib» под шапкой сжата до 10 px; дата «14 Sentyabr 2026, Bazar Ertəsi» с заглавными (в строке выше — строчными); у Store 2 адрес «Baku» в каталоге становится «Ünvan göstərilməyib» после сохранения.
- [x] **B9** Экран «Маршрут» без маршрута — P1 · [rashadoni/leaddrive-mtm #7](https://github.com/rashadoni/leaddrive-mtm/pull/7) merge `fb132ed0` · S-plan · принято на телефоне 2026-09-13
  - Код влит. Степпер «Шаг 1 из 5: Точка» рисовался при надписи «Сегодня маршрута нет» — пять шагов маршрута, которого нет. Теперь появляется только вместе с маршрутом, в обеих раскладках.
  - Обновление предлагалось дважды подряд: кнопка «Обновить маршрут» и над ней «или потяните для обновления». Осталась кнопка — на пустом экране тянуть нечего, а жест не виден; текст пустого состояния переписан на трёх языках.
  - Карточка «Нужен визит вне маршрута? Откройте „Визиты“» убрана: она была половиной кольца, вторая половина стоит на «Визитах» и отправляет обратно. Внеплановый визит — кнопка в шапке. Карточка «Составить мой маршрут» осталась: этот экран отсюда больше ниоткуда не достать.
  - **Принято на телефоне 2026-09-13** (build56). На «Marşrut» без маршрута: степпера нет, «Bu gün marşrut yoxdur», одно предложение обновить — кнопка «Marşrutu yenilə», карточки-кольца на «Визиты» нет, экран в один экран.
  - Найдено, не хвост B9: внизу стоит закрываемая подсказка «Yeniləmək üçün siyahını aşağı çəkin…» — второе предложение обновить, от которого B9 уходил; это общий механизм подсказок, а не разметка экрана. На «Ziyarətlər» обратная половина кольца («Ziyarət artıq plandadır? → Marşrutu aç») осталась.
- [x] **B10** Задачи — P1 · [rashadoni/leaddrive-mtm #8](https://github.com/rashadoni/leaddrive-mtm/pull/8) merge `91f6db89` · S-plan · принято на телефоне 2026-09-13
  - Код влит. Заголовком экрана было «Понятный порядок выполнения» — над названием задачи, ради которого экран и открывают. Убран.
  - Три раздела начинались с наставления («Сначала прочитайте задачу…», «Проверьте, где и до какого времени…», «Отмечайте продвижение…»), и завершённая задача получала те же уроки, что и не начатая. Убраны. Строка у «Подтверждения и файлов» оставлена: она описывает содержимое, которое по заголовку не угадать.
  - Приоритет писался советом «Высокая · не откладывайте». Первую половину экран знает, вторую — нет. Фильтры «1 · 2 · 3» перестали выглядеть шагами процедуры: это три вида одного списка.
  - **Принято на телефоне 2026-09-13** (build56). Над списком нет слогана; приоритет «Yüksək» без совета; в карточке задачи разделы «Nə etmək lazımdır» и «Son tarix və müştəri» начинаются без наставлений.
  - Найдено, не хвост B10: шапка экрана осталась пустой зелёной полосой (0–190 px) без названия «Tapşırıqlar»; заголовок пустого состояния «Yeni tapşırıq yoxdur» обрезан до «Yeni tapşırıq» (вторая строка не помещается в 65 px); у выполненной задачи красный баннер «Sinxronizasiyanı yoxlamaq lazımdır» отсылает к «индикатору выше», которого на экране нет; нижняя панель пронумерована «5» при видимых разделах 1 и 2.
- [x] **B11** «Ещё» и профиль — P2 · [rashadoni/leaddrive-mtm #9](https://github.com/rashadoni/leaddrive-mtm/pull/9) merge `6a9b9fce` · S-plan · принято на телефоне 2026-09-13
  - Код влит. Над меню из четырёх ссылок стояло пять строк вступления; осталось одно название, и оно перестало быть вопросом — вопрос предлагает ответить, а экран принимает нажатие.
  - Профиль повторял три вещи: «Показатели за сегодня» (дубль главной, обе половины показывали нули; дневные числа живут в карточке выбранного дня календаря после B7), «Моя GPS-история» (есть в «Ещё») и чип синхронизации. Чип стоял в четырёх местах сразу — «Сегодня», профиль, шапка каждой карточки задачи и центр за ними; четыре индикатора это четыре возможности разойтись об одном факте. Остался один.
  - Чип в `DashboardScreen.android.tsx` не тронут: это менеджерская оболочка, из Route Field недостижимая, и тест навигатора это сторожит.
  - **Принято на телефоне 2026-09-13** (build56). «Daha çox» — одно название и четыре ссылки; профиль без «показателей за сегодня» и без дублей: контакты, подключение, язык.
- [x] **B12** Центр синхронизации словами пользователя — P2 · [leaddrive-mtm #87](https://github.com/rashadrahimov/leaddrive-mtm/pull/87), [rashadoni/leaddrive-mtm #1](https://github.com/rashadoni/leaddrive-mtm/pull/1) merge `5367adbd` · S-plan · в сборке 3.1.0/43
- [x] **B13** Верхний регистр в азербайджанском — P1 · [leaddrive-mtm #74](https://github.com/rashadrahimov/leaddrive-mtm/pull/74), [#76](https://github.com/rashadrahimov/leaddrive-mtm/pull/76) · S-sprint1 + S-plan · сверено с кодом 2026-09-06
- [x] **B14** Русские плюралы — P1 · [leaddrive-mtm #74](https://github.com/rashadrahimov/leaddrive-mtm/pull/74), [#76](https://github.com/rashadrahimov/leaddrive-mtm/pull/76) · S-sprint1 + S-plan · сверено с кодом 2026-09-06
  - Хвост закрыт: [rashadoni/leaddrive-mtm #1](https://github.com/rashadoni/leaddrive-mtm/pull/1), merge `5367adbd`. Оказался не тем, чем выглядел: два ключа из трёх не отрисовывались нигде, третий жил только в тесте. Настоящий дефект был рядом — экраны задач несли собственные трёхъязычные словари в компонентах, и при одном изменении агент читал «Ждут отправки: 1». Ключ не склонён, а оживлён; мёртвые удалены.
- [x] **B15** Сырые значения в интерфейсе — P1 · [leaddrive-mtm #74](https://github.com/rashadrahimov/leaddrive-mtm/pull/74) · S-sprint1 · сверено с кодом 2026-09-06
- [x] **B16** Карта GPS без водяного знака — P1 · [leaddrive-v2 #1085](https://github.com/rashadrahimov/leaddrive-v2/pull/1085), [leaddrive-mtm #74](https://github.com/rashadrahimov/leaddrive-mtm/pull/74) · S-sprint1 · сверено с кодом 2026-09-06 · **веб проверен на проде 2026-09-10** · приложение проверено на телефоне 2026-09-13
  - **Ключ уже стоит, и это не предположение.** Секрет `CARTO_BASEMAPS_API_KEY` заведён в `rashadoni/leaddrive-v2` 2026-09-09, а в `deploy.yml` есть шаг, который валит сборку при пустом значении (`test -n "${NEXT_PUBLIC_CARTO_BASEMAPS_API_KEY:-}" || exit 1`). Раз деплои проходят, ключ на месте — проверка не требует чтения самого значения.
  - **Веб-половина закрыта фактом.** На проде открыта «Canlı Xəritə» через браузер владельца: загрузилось 12 тайлов `basemaps.cartocdn.com`, у **каждого** в URL есть `?key=`, на экране чистая карта Баку без надписи «API KEY REQUIRED».
  - **хвост:** половина в приложении. Ключ едет в WebView карты GPS-истории через `bootstrap` → `maps.cartoBasemapsApiKey`, и проверяется это только на телефоне (эпик D). Там же проверяется, что allowlist ключа в кабинете CARTO покрывает origin приложения: ради этого в T12 чинился `referrerPolicy` — он стоял `no-referrer`, и CARTO не видела, откуда пришёл запрос, так что привязка к домену не сработала бы вовсе.
  - **Замер на телефоне 2026-09-13 (build56): водяного знака нет, но и карты нет.** На «GPS tarixçəm» вместо тайлов значки битых картинок и статус «Xəritə əlçatan deyil. GPS marşrutu ehtiyat tor üzərində görünür». Это не отсутствие ключа: без ключа CARTO отдаёт картинку с надписью «API KEY REQUIRED», и она загружается. Здесь срабатывает `onerror` — ключ пришёл и отклонён. WebView ставит `baseUrl` на сервер тенанта, и тайлы уходят с `Referer: https://leaddrive.leaddrivecrm.org/`; на вебе тот же ключ работает с `app.leaddrivecrm.org`. Значит, в allowlist ключа в кабинете CARTO нет домена тенанта.
  - **Блокер у владельца:** добавить `leaddrive.leaddrivecrm.org` (или `*.leaddrivecrm.org`) в разрешённые домены ключа в кабинете CARTO. Код менять не нужно; после этого — повторить замер на телефоне. Попутно на карте подпись «Seçilmiş» обрезана и наезжает на соседнюю.
  - **Блокер снят владельцем и проверен на телефоне 2026-09-13:** владелец добавил основной домен в кабинете CARTO — отдельно поддомены добавлять не пришлось. На «GPS tarixçəm» карта Баку с улицами и зданиями, маршрут поверх, водяного знака нет.
  - Хвост закрыт: [rashadoni/leaddrive-mtm #1](https://github.com/rashadoni/leaddrive-mtm/pull/1), merge `5367adbd`. `referrerPolicy` стал `origin`, и ключ CARTO снова можно привязать к домену тенанта.
  - Хвост закрыт: [rashadoni/leaddrive-mtm #1](https://github.com/rashadoni/leaddrive-mtm/pull/1), merge `5367adbd`. Шесть случаев на путь ключа — разбор `maps.cartoBasemapsApiKey` из bootstrap, `?key=` в сгенерированном HTML, экранирование ключа, отсутствие пустого `?key=` и достижимость origin тенанта.
- [x] **B17** Диалог завершения дня — P2 · [rashadoni/leaddrive-mtm #11](https://github.com/rashadoni/leaddrive-mtm/pull/11) merge `17af6f35` · S-plan · принято на телефоне 2026-09-13
  - Код влит. Порядок кнопок в коде был правильный и до правки — так их раскладывает сама система: на Android вторая становится положительной и уезжает вправо, под большой палец. Системный диалог здесь нельзя починить, можно только не использовать. День закрывается через собственный `ConfirmSheet`.
  - Тем же листом заменены три информационных Alert'а на этом же экране (`hideCancel`); импорт `Alert` из `TodayScreen` ушёл целиком.
  - **Принято на телефоне 2026-09-13** (build56). «Günü bitir» открывает лист приложения, а не системный Alert: «İş gününü bitirirsiniz?», «İşə davam et» слева, красная «Günü bitir» справа, лист над системной панелью. Нажата «İşə davam et» — рабочий день продолжается.
- [x] **B18** Доступность — P2 · [rashadoni/leaddrive-mtm #13](https://github.com/rashadoni/leaddrive-mtm/pull/13) merge `ef08519f` · S-plan · принято на телефоне 2026-09-13
  - Код влит. Первая половина была верна и раньше (описания для скринридера без глифа); осталось 12 px вместо 10. Решает самая длинная подпись, а она азербайджанская — «Tapşırıqlar», одиннадцать знаков; `letterSpacing` не давал ничего и стоил примерно знака места, убран.
  - Попутно найдено расхождение, которое было там и до правки: `useTabBarPadding` считал панель высотой 56, навигатор рисовал 60 — нижняя строка каждого экрана сидела под панелью. Высота теперь одна константа `TAB_BAR_BASE_HEIGHT`.
  - **Приёмка 2026-09-13 нашла, что вкладок не видно вовсе, — и это была регрессия самого B18.** `TAB_BAR_BASE_HEIGHT` жил в `hooks/useTabBarHeight.ts`, а Metro на Android берёт `useTabBarHeight.android.ts`, где его не было: высота панели `NaN`, стиль отброшен, панель схлопнулась до отступов (157 px, кнопки 28 px, подписи нулевой высоты). Jest резолвит `.ts` — тесты были зелёными. Вдобавок Android 16 при `targetSdk 36` рисует окно под системной панелью, и нажатие по иконке уходило в «Назад/Домой/Недавние».
  - Исправлено [rashadoni/leaddrive-mtm #18](https://github.com/rashadoni/leaddrive-mtm/pull/18), merge `0a23be79`, релиз `v3.1.0-build56`: константа в `theme/tabBarMetrics.ts` без двойника; тест обходит все пары `x.ts`/`x.android.ts` и требует одинаковых экспортов; `MainActivity` отступает content view на высоту панели навигации.
  - **Принято на телефоне 2026-09-13** на build56: панель `y=1981..2181` над системной (2181..2316), кнопки 155 px, подписи 43 px. AZ «Bu gün / Təqvim / Marşrut / Tapşırıqlar / Daha çox», RU «Сегодня / Календарь / Маршрут / Задачи / Ещё», EN «Today / Calendar / Route / Tasks / More» — каждая в границах своей кнопки. Язык после проверки возвращён на AZ.
- [x] **B19** Планшет от 600 dp — P1 · [rashadoni/leaddrive-mtm #16](https://github.com/rashadoni/leaddrive-mtm/pull/16) merge `a6862eac` · S-plan · принято на телефоне 2026-09-14
  - Код влит. Причина оказалась не в вёрстке колонки: у продукта было два ответа на вопрос «это планшет?», и они расходились ровно посередине. Рельс навигации, экран маршрута и база переключались на 600, календарь и задачи ждали 840. Телефон в ландшафте — 824 dp — попадает в щель, поэтому рельс появлялся, а содержимое оставалось одной узкой колонкой. Ровно это аудит и записал.
  - Две панели живут под одним именем `isTwoPaneWidth` (600). Шестисот достаточно: при делении 42/58 списку остаётся около 250 dp. У списка дня появился нижний предел ширины, у детали `minWidth: 0`, чтобы длинное имя сжимало текст, а не ломало раскладку.
  - 840 остался и отвечает на свой вопрос — не «помещаются ли две панели», а «может ли панель позволить себе просторность»: отступы, цели нажатия, расширенный рельс. Экраны, которые уже были правы, не тронуты.
  - **Принято на телефоне 2026-09-14 в ландшафте** (владелец повернул телефон; окно 823×384 dp): «Təqvim» — две панели, список дней слева и выбранный день справа.
  - **Дефект, найден на экране, исправлен и принят:** боковая навигация в ландшафте была 82 dp (112 — только от 840), подписи читались «B…», «T…», «M…». [rashadoni/leaddrive-mtm #25](https://github.com/rashadoni/leaddrive-mtm/pull/25), merge `eb0fe36d`: одна ширина 124 dp (при 112 на телефоне ещё было «Tapşırı…» — поле подписи 51 dp). На телефоне: AZ «Tapşırıqlar» 226–391 px, RU «Календарь» 221–395 px — все подписи целиком.
  - Планшет в портрете не проверялся: устройства нет; телефонный ландшафт — ровно та ширина между 600 и 840, из-за которой B19 и заводился.
- [ ] **B20** Экран входа — P1 · [rashadoni/leaddrive-mtm #14](https://github.com/rashadoni/leaddrive-mtm/pull/14) merge `d09aec54` · S-plan
  - Код влит. Замер на телефоне 2026-09-10: первое поле начиналось на y≈1663 из 2316, то есть на 72 % высоты. Над ним стояли марка, название, заголовок приветствия, абзац и список из двух шагов — а список говорил ровно то, что и два блока под ним: строка прогресса даёт шаг, карточка компании даёт компанию и «Изменить».
  - На телефоне остались марка и название, и они встали в строку. Приветствие, абзац и список — только на планшете, где вступление занимает свою колонку. Каре сразу в первом поле, на обоих экранах шага 1 и шага 2.
  - **хвост:** приёмка на телефоне: поля и кнопка видны без прокрутки.
- [x] **B21** История визитов на «Визитах» открывается — P2 · [rashadoni/leaddrive-mtm #12](https://github.com/rashadoni/leaddrive-mtm/pull/12) merge `ab626d27` · S-plan · принято на телефоне 2026-09-13
  - Код влит. Одна и та же запись жила в двух списках с разным поведением: в карточке организации строка открывает «Итог визита», на экране «Визиты» не делала ничего. Теперь открывает то же и тем же путём, в обеих раскладках.
  - Строка, которую открыть нечем (визит из очереди без серверного id), остаётся обычной строкой — признак решает и обёртку, и роль для скринридера, и стрелку. Кнопка, которая ничего не делает, — ровно тот дефект, ради которого задача заведена.
  - **Принято на телефоне 2026-09-13** (build56). Строка истории «Bravo Market, Tamamlanıb» на «Ziyarətlər» открывает «Ziyarətin yekunu»: вход 13:28, выход 13:56.
  - Найдено, не хвост B21: в итоге визита под подписью «Müştəri» стоит адрес («Ünvan göstərilməyib»), а не имя; в списке клиентов на «Ziyarətlər» имена обрезаны до «ADV-DE…», потому что чип «Koordinatlar göstərilməyib» забирает ширину строки.
- [x] **B22** Сборки — P2 · [rashadoni/leaddrive-mtm #15](https://github.com/rashadoni/leaddrive-mtm/pull/15) merge `dcbd20f6` · S-plan · проверено на прогоне main
  - Опасность была не гипотетической: ветка `claude/appetize-x86_64` существует в новом репозитории, а workflow собирает APK на каждый пуш в `claude/**` и публикует пререлиз с тегом того же вида, что настоящий. APK с x86_64 не встанет ни на один полевой телефон.
  - Гейт спрашивает у самого APK, под какие процессоры он собран (`unzip -Z1 … lib/*`), а не смотрит на имя ветки: имя ветки — это то, что забудут. Эмуляторный ABI без явной просьбы — красный шаг; просьба есть, вход `emulator_build` у `workflow_dispatch`, и при нём релиз не публикуется вовсе. Обратное направление тоже закрыто: сборка без `arm64-v8a` — тоже красный шаг.
  - Проверено фактом: прогон main на `dcbd20f6` напечатал «Native ABIs: arm64-v8a armeabi-v7a».

### Эпик C — Веб `/mtm/*` · закрыто 16 из 16

- [x] **C1** Имя клиента из текстов продукта — P0 · [leaddrive-v2 #1086](https://github.com/rashadrahimov/leaddrive-v2/pull/1086) · S-sprint1 · сверено с кодом 2026-09-06 · хвосты сняты 2026-09-10
  - Хвост снят: комментарии в `src/lib/social/*` имени клиента больше не несут — проверено 2026-09-10, гейт `product-copy-client-names` покрывает `src/lib` целиком и зелёный.
  - Хвост закрыт: `SWM_PLAN_GPS_V1` уходил на экран аналитики бейджем «Formula SWM_PLAN_GPS_V1» (W-11). Две беды сразу: идентификатор там, где место названию, и приставка из инициалов клиента — то самое, ради чего C1 и заведена. Бейдж теперь называет расчёт словами на трёх языках, код переехал в подсказку, рядом с бейджем политики, который так работает с T14. Саму константу не переименовывал: по ней сравнивает API, и её несут уже записанные строки аудита — переименование переписало бы историю, до которой не дотянуться.
  - Хвост снят и переформулирован, потому что в прежнем виде он неисполним. Зелёного прогона по PR #1086 не будет никогда: репозиторий, где он жил, удалён. Но вопрос за хвостом — «проверено ли это CI» — теперь отвечается лучше, чем одним прогоном: свойство сторожит гейт `product-copy-client-names`, он идёт в блокирующем шаге «Unit tests vs baseline» на каждом PR и **не числится в базлайне**. Именно поэтому его зелёность доказуема: покрасней он, счётчик стал бы 19 из 18 и мерж бы не прошёл. Проверено на прогоне 2026-09-10.
  - Не хвост, а граница задачи: `scripts/` (`swissmed-mtm-browser-evidence.mjs`, `seeds/zeytunpharm-swissmed-evidence.mjs`) и `.github/workflows/swissmed-mtm-browser-evidence.yml` имя клиента сохраняют намеренно. Это QA-оснастка, а не тексты продукта, и так записано в самом гейте: «QA fixtures and tests keep their names; they are not product copy». Оставлять это открытым хвостом значит держать C1 незакрываемой ради решения, которое уже принято.
- [x] **C2** Даты только через хелпер — P0 · [leaddrive-v2 #1087](https://github.com/rashadrahimov/leaddrive-v2/pull/1087) · S-sprint1 · сверено с кодом 2026-09-06 · S-plan · хвосты сняты 2026-09-08
  - Хвост закрыт: `formatInTimezone` идёт через `createDateFormatter`. Это была дыра насквозь: одиннадцать компонентов MTM читают сроки задач, историю GPS и черновики маршрутов через неё, поэтому один прямой вызов Intl отменял азербайджанскую правку сразу во всех экранах, которые её честно применили. Тест сравнивает вывод en/ru/en-US байт в байт с прежним `Intl.DateTimeFormat`, а для `az` требует «14 may 2026, 16:00» вместо «2026 M05 14».
  - Хвост закрыт: `dayLabel` идёт через `formatDate`. Это был последний прямой `toLocaleDateString` на пути чтения журнала активности; «Сегодня» и «Вчера» остались словами.
  - Хвост закрыт наполовину, и намеренно: гейт теперь сканирует ещё и два библиотечных файла поимённо (`src/lib/timezone.ts`, `src/lib/mtm/activity-actions.tsx`) — те, через которые экраны MTM достают даты. Расширять его на весь `src/lib` не стал: там форматируют машинные значения (YYYY-MM-DD для `<input type="date">`, строка смещения под разбор), где приколоченная локаль и есть правильный ответ, а гейт, который ругается на правильный код, учит его глушить. Три таких вызова в `timezone.ts` помечены маркером с причиной.
- [x] **C3** Язык страницы из локали — P1 · [leaddrive-v2 #1086](https://github.com/rashadrahimov/leaddrive-v2/pull/1086) · S-sprint1 · сверено с кодом 2026-09-06
  - Хвост закрыт: аварийная страница объявляет язык и говорит на нём. `global-error.tsx` заменяет корневой layout, поэтому провайдера локали там нет по определению — он упал вместе с ним. Язык берётся из того же cookie `NEXT_LOCALE`, что пишет переключатель, затем из `navigator.languages`, затем продуктовый по умолчанию; каждый шаг тотальный, потому что страница, которая падает при падении, оставляет пустое окно. Две фразы лежат в модуле, а не в `messages/*.json`: загрузка словаря — ровно та работа, которой уже нет, когда layout сломан.
- [x] **C4** «Не завершён» на вебе — P0 (вместе с A3) —  · [leaddrive-v2 #1092](https://github.com/rashadrahimov/leaddrive-v2/pull/1092) · S-plan · сверено с кодом 2026-09-06
  - Хвост закрыт: веб-часть C4 закреплена своим контрактом (`mtm-route-incomplete-web-ui-contract`). Раньше держалось только существование ряда фильтров — ни `INCOMPLETE` в нём, ни строка причины на карточке не были защищены ничем, и обе можно было удалить, не уронив ни одного теста. Проверено мутацией: убираешь статус из фильтров — тест краснеет; убираешь строку причины — краснеет. Отдельно закреплено, что причина не повторяет подпись чипа: «Не завершён» отвечает на «что» и оставляет «почему» догадкам, ради этого строка и существует.
- [x] **C5** Первый экран — P1 · S-plan · сверено с кодом 2026-09-08 · хвосты сняты 2026-09-10
  - Сделано: гайд «3 addım» научился прятаться — кнопка «больше не показывать», выбор запоминается по зрителю и по панели (`src/lib/mtm/dismissible-hint.ts` под тестами, включая случай, когда хранилище браузера бросает исключение). Подключён на «Визитах».
  - Хвост закрыт: гайд теперь закрывается на всех пяти экранах, а не только на «Визитах». Четыре остальных места рисовали ту же панель вообще без кнопки — панель, существующая чтобы научить один раз, стояла над работой всегда. У каждого экрана свой `dismissId`: общий означал бы, что закрыл на контактах — пропало на промо-акциях. Зритель выводится одним `mtmViewerKey`, а не четырьмя копиями строки; запасное значение оставлено пустым, потому что как назвать «никого» решает `mtmHintStorageKey`, и дублировать это решение по вызовам — та же ошибка, что чинила C8.
  - Хвост закрыт, обе половины, и обе проверены на экране владельца через расширение Chrome (это первая часть D2, выполненная фактом).
  - **Замер 2026-09-10, 1470×675** — ширина из самого аудита: первая строка данных начиналась на `y=312`, то есть 46 % окна уходило на шапки. Раскладка сверху: приложение 56 px, полоса модуля 57, заголовок страницы 63, шапка карточки ~70.
  - AI-строки на этой странице уже нет — снята раньше; проверено в дереве страницы, а не по коду.
  - Заголовок сведён в одну строку: подзаголовок «Aya baxın, günü planlaşdırın və icranı izləyin.» был четвёртым местом, говорящим одно и то же — полоса модуля подсвечивает «Marşrutlar», заголовок повторяет, а статья за «?» объясняет страницу целиком. По DoD объяснение живёт за «?», там и осталось. Ключ удалён из трёх словарей, чтобы не остался «наверное, где-то показывается».
  - **Поправка к самому себе, замерено после выкатки.** Высоты на 1470 px: шапка 63, блок заголовка 32, панель инструментов 50. Шапка — это flex-строка, и её высоту задаёт панель инструментов, а не заголовок. Значит на широком десктопе снятие подзаголовка **не дало вертикали почти ничего**. Выигрыш там, где шапка складывается в столбец, — ниже 1280 px (`xl:flex-row`), то есть на тринадцатидюймовом ноутбуке, про который аудит и писал. Само снятие дубля остаётся верным по правилу 5 DoD независимо от пикселей, но приписывать ему экономию места на десктопе было бы неправдой.
- [x] **C6** Ячейки календаря — P1 · S-plan · сверено с кодом 2026-09-09
  - Сделано: «+» появляется по hover и по фокусу с клавиатуры и **всегда виден на touch** (`[@media(hover:none)]:opacity-100`) — на планшете кнопки, которую видно только под курсором, не существует. Прошедшие дни приглушены (`isPastMtmCalendarDay`, `data-past-day` для теста), и приглушение считается только внутри текущего месяца, чтобы соседние месяцы не выглядели «прошедшими» дважды.
  - Подпись нерабочего дня берётся из общего словаря (`mtmStatus.dayKind`), а не из собственных ключей календаря: `dayKind` уже различает государственный праздник, корпоративный выходной и перенесённый день, а «нерабочий день» свалил бы их в самое расплывчатое из трёх. Своё имя переопределения по-прежнему сильнее вида.
  - Хвост закрыт: нерабочие дни затенены и **называют причину прямо в ячейке**, а не только в `title` — на планшете подсказки под курсором не существует. Выходной по умолчанию отличается от переопределения: у праздника показывается его собственное название, «Нерабочий день» остаётся запасным для переопределения, сохранённого без имени. Затенение включено только при `enforceWorkCalendarForRoutes`: пока тенант его не включил, суббота ничего не запрещает, и серая суббота была бы правилом, которого у продукта нет. Запрос переопределений деградирует — эндпоинт стоит за модулем Workforce HRM, и тенант без него должен получить рабочий календарь, а не пустой экран.
- [x] **C7** Командная неделя — P1 · S-plan · сверено с кодом 2026-09-09 · хвосты сняты 2026-09-10
  - Сделано: QA-аккаунты (`[QA-`) скрыты, и рядом стоит счётчик скрытых — беззвучный фильтр пугает сильнее, чем строка «скрыто 3». Поиск по сотруднику складывает диакритику через NFD, поэтому «ƏLİYEVA» находит «Əliyeva»: `toLocaleLowerCase("az")` для этого не годится, он превращает `I` в `ı`. Дни недели идут через общий хелпер, а не через `toLocaleDateString`.
  - Хвост закрыт: фильтр по команде есть (RUX-602). Разобрано так, как и было записано, — не в вёрстке. Команда теперь едет с именем агента по всем трём путям: `routeAgentSelect` один на `route.agent` и на `assignment.agent`, поэтому разойтись снова они не могут. Фильтровать «только среди пришедших из списка» было вторым вариантом и оказалось хуже: он молча выбрасывал бы ровно ту половину, которую руководитель и ищет, — людей, попавших в неделю через маршрут.
  - Выбор предлагается только среди команд, у которых в этой неделе есть строка, и только когда команд больше одной: одна команда — это не выбор, а лишний элемент управления.
  - Хвост закрыт: у прокручиваемой сетки появилась подсказка, что справа есть ещё. Аудит записал «обрезаны без намёка на прокрутку», и это читается как пятидневная неделя, а не как прокручиваемая.
  - Сделано на чистом CSS, без слушателя: пара `background-attachment: local, local, scroll, scroll` двигает закрывающие градиенты вместе с содержимым, поэтому тень открывается ровно с той стороны, где содержимое продолжается, и сама исправляется при прокрутке и изменении размера. Слушатель ресайза ради визуальной подсказки — это то, что переживает свой компонент и течёт.
  - Цвета взяты из токенов темы, поэтому тень не ломается в тёмной.
- [x] **C8** Планировщик — P1 · S-plan · сверено с кодом 2026-09-08 · закрыто 2026-09-13 фактом на проде
  - Сделано: ключ автосохранения теперь «день + сотрудник» (или id маршрута), а не «сотрудник + дата + клиент + контакт». Из-за прежней схемы один и тот же вторник существовал под разными ключами, и приложение предлагало тот черновик, чей ключ случайно совпал, — так и получилось «восстановился 09:43 вместо 15:23». При нескольких кандидатах побеждает самый свежий `savedAt`; ключи прежней схемы читаются, чтобы ничего не потерялось. Логика в `src/lib/mtm/route-draft-storage.ts` под тестами.
  - **Два пункта хвоста сняты на проде 2026-09-10** (D2, планировщик открыт в браузере владельца; ничего не сохранялось и не публиковалось).
  - **scroll-lock есть.** При открытом планировщике `body` стоит `overflow: hidden`, а `document.documentElement.scrollHeight` равен высоте окна (743 = 743) — страница за модалом прокрутиться не может.
  - **Шаг 1 не молчит, и молчал он иначе, чем записано.** Дело не в том, что нажатие не даёт реакции: рядом с главной кнопкой постоянно висит полоса `role="status" aria-live="polite"`, которая называет недостающее прямым текстом — «Marşrutun hansı əməkdaşın tətbiqində görünəcəyini göstərin». Это лучше валидации по нажатию: подсказка видна до того, как человек ткнул в кнопку, и её слышит диктор. `aria-invalid` у поля нет — и правильно: состояния ошибки нет, есть невыполненный шаг.
  - Единственный шрифт мельче 12 px на шаге 1 — цифра «1» в кружке степпера (10 px). Это не «карточки каталога кеглем 10–11 с обрезанным текстом» из аудита: тот пункт живёт на шаге 2.
  - **Шаг 2 пройден на проде 2026-09-10.** Ничего не сохранялось: выбор сотрудника, точки и отмена — всё это состояние клиента, черновик появляется только при сохранении, к которому я не прикасался.
  - **Одна главная кнопка на шаге 2 — сделано.** Аудит видел две одинаковые «Marşrutu yoxla» (панель и футер). Сейчас на шаге ровно одна с `data-testid="mtm-route-primary-action"`; остальное, что выглядит «главным», — это карточки кандидатов и чипы типов, то есть выбираемые элементы, а не действия.
  - **Кегль карточек каталога — дефект воспроизводился, закрыт.** Замер: значок категории 10 px, строка «Son ziyarət: …» 11 px, по восемь вхождений каждого. Последний визит — не украшение: по нему решают, кого добавить в маршрут. Оба подняты до 12 px — до того же порога, который этот план сам себе назначил, когда поднимал подписи вкладок в приложении (B18, RUX-703). Числовые бейджи (цифра шага, счётчик фильтров) оставлены мелкими намеренно: цифра в кружке — метка, а не текст.
  - **Отмена без подтверждения — дефект воспроизводился, закрыт.** Проверено буквально: выбран один клиент, нажата «Ləğv et», планировщик закрылся, диалога не было. Теперь закрытие с непустым выбором спрашивает и называет, сколько клиентов потеряется; пустой планировщик по-прежнему закрывается одним нажатием. Через ту же проверку идут обе двери — и футерная кнопка, и крестик.
  - Автосохранение это смягчало, но не оправдывает: аудит уже ловил, что восстановление поднимает **более старый** черновик, — то есть «оно потом восстановится» не аргумент против того, чтобы спросить.
  - ~~Туда не ходил: путь к нему лежит через сохранение черновика в живой сессии владельца.~~ **Посылка была неверной.** «Marşrutu yoxla» только переключает шаг (`focusStep(3)`), автосохранение пишет в `localStorage`, а в базу уходит лишь главная кнопка самого шага 3. Проверено 2026-09-13 на проде: пройдены шаги 1–3, перехват `fetch` не зафиксировал ни одного не-GET запроса.
  - **Публикация без времён — дефект воспроизводился, исправлен кодом, ждёт проверки на проде.** На шаге 3 у двух встреч стояло «—», кнопка «Saxla və dərc et» была активна, и по коду нажатие шло прямо в `save("publish")`. Сервер `plannedTime: null` принимает, так что встречи без времени одним нажатием уходили в приложение сотрудника. Кнопка «Boş vaxtları 09:00-dan doldur» стояла рядом, но пройти мимо неё ничто не мешало.
  - Теперь публикация при хотя бы одной встрече без времени открывает подтверждение: оно называет, сколько встреч из скольких без времени, говорит, что сотрудник увидит их без времени, и указывает на кнопку автозаполнения. Черновик не спрашивает: его не видит никто, кроме планировщика. Пользователь без права публикации сохраняет черновик тоже без вопроса.
  - **Принято на проде 2026-09-13** ([#156](https://github.com/rashadoni/leaddrive-v2/pull/156), merge `e841c3f4`, деплой [34758222923](https://github.com/rashadoni/leaddrive-v2/actions/runs/34758222923), `.deploy-sha` и `build-info` сошлись на `e841c3f4`). Шаг 3, у обеих встреч время «—», нажата «Saxla və dərc et». Открылось «Vaxtsız dərc edilsin? — 2 görüşdən 2 görüşün vaxtı yoxdur…» с кнопками «Ləğv et» и «Vaxtsız dərc et», и лежало оно поверх планировщика (`elementFromPoint` в центре «Ləğv et» попадает в саму кнопку). «Ləğv et» закрыло подтверждение, планировщик остался на шаге 3.
  - Ничего не опубликовано, и не по доверию к новому коду: на время проверки в окне был подменён `fetch`, и любой не-GET запрос, кроме `meeting-availability`, отклонялся. Отклонённых оказалось ноль, то есть кнопка до сервера даже не дошла. Так страховка работала бы и в случае, если service worker отдал старый бандл.
- [x] **C9** Индикатор вида — P2 · S-plan · закрыто 2026-09-10 фактом на проде, ответ владельца не понадобился
  - **Вопрос снят, потому что был задан не туда.** Прежняя запись гласила: «источник требования — RUX-106, которого в репозитории нет». Он есть — `docs/mtm-routes-ux-roadmap-2026-08-23.md`, строка 116: «Replace duplicated status chips/counters with one filter model / Status count has one source and one interaction». Это предмет **C11**, а не индикатор вида, и C11 закрыт. Значит RUX-106 — просто неверная ссылка у C9.
  - **Настоящее требование C9 — это W-08 самого аудита:** «Два элемента одновременно выглядят выбранными. При активном календаре выпадающая кнопка подписана „Bütün marşrutlar“, последним выбранным вторичным видом».
  - **Проверено на проде 2026-09-10** через браузер владельца, ровно тем путём, который порождал дефект: календарь → «Nəzarət və hesabatlar»; матрица → «Həftə planı»; назад на календарь → снова «Nəzarət və hesabatlar», у календаря `aria-pressed=true`. Подпись выводится из `viewMode` при каждом рендере и нигде не запоминается, поэтому застрять она не может. Одновременно выбранным читается ровно один элемент.
  - Индикатор при этом тройной, и одного из трёх не хватило бы: подпись называет активный вид, `aria-pressed` читает диктор, рамка в цвете акцента видна тому, кто скользнул взглядом по строке. Тест дополнен случаем на сам W-08 — возврат к общей подписи и отсутствие запомненного состояния.
- [x] **C10** Карточка маршрута — P1 · S-plan · сверено с кодом 2026-09-08
  - Блок «Advisor riski» убран с карточки маршрута (RUX-604); в задачах и платежах он остаётся, там его читают осознанно. Пустое состояние карты при `null`-координатах уже было сделано в спринте 1 (`mtm-route-map-empty`) — проверено, не переписывал. Оба условия закреплены тестом.
- [x] **C11** Счётчики задач — P1 · S-plan · сверено с кодом 2026-09-08
  - Счётчики статусов теперь из того же отфильтрованного набора, что и «Всего»: API отдаёт `summary` через `groupBy` по тому же `where`. Раньше рядом стояли серверное «Всего: 137» и статусы, посчитанные по строкам текущей страницы, — ни одно число не было неверным, врали они вместе.
  - Срок задачи, попадающий ровно на местную полночь, показывается без времени: «00:00» выглядело как выбранный час, хотя это дата без времени.
- [x] **C12** Служебная лексика — P1 · S-plan · сверено с кодом 2026-09-08
  - Журнал активности печатал имя таблицы и шесть символов идентификатора («mtm_pharmacy_promotion_execution · 4f2a9c»). Теперь объект называется словом, а полный id уехал в подсказку — по обрывку всё равно ничего не найти, а поддержке нужен целый.
  - Строки свежести данных в KPI проверены: они уже человеческие («Факты в доступном наборе не менялись более 15 минут»), формулировки «источник не менялся» в коде нет. Не переписывал.
  - **Пересмотрено (T14).** Прошлый проход оставил бейдж `{code} · версия {version}` намеренно: код утверждённой политики — ссылка на правило, по которому считали. Довод верен, но посылка была неполной: у `MtmKpiPolicy` есть `nameRu`, `nameAz` и `nameEn`, заполняемые при создании, и API их просто не отдавал — выбора между кодом и именем не было, было отсутствие имени. Теперь бейдж называет политику словами читателя, а код с ссылкой на утверждение переехал в подсказку: он никуда не делся, им по-прежнему можно сослаться. Пустое имя считается отсутствующим (колонки непустые, импорт из другой системы приносит `""`), дальше английское, дальше код.
  - **Ждёт слова владельца:** это видимая пользователю формулировка, слой 4 `DELIVERY-ARCHITECTURE.md`.
  - Оповещения (`alerts/page.tsx`) закрыты задачей A4.
- [x] **C13** Рейтинг — P2 · S-plan · 2026-09-10
  - **Ловушка в приёмке.** «Мёртвого кода не остаётся» читается как «удалить `BubbleArena`». Удалять нельзя: тот же компонент и его скелет держат общую страницу рейтинга `src/app/(dashboard)/leaderboard/page.tsx`. С MTM-страницы убирается только применение — вид, переключатель и холст.
  - ~~**Развилка, которую я не решаю за владельца.**~~ **Развилки нет — прежняя запись опиралась на неверный факт.** Утверждалось: «это разные формы, общего ключа у них в ответе нет». Ключ общий: `MtmRanking.agentId` и `NormalizedAgent.id` — это один и тот же id агента, и arena-форма **выводится из тех же строк рейтинга** чистой функцией `mtmToNormalized(rankings)` в `src/lib/leaderboard/mtm.ts:184` (`id: r.agentId`). Заголовок модуля говорит это прямым текстом: логика извлечена из `/api/v1/mtm/leaderboard`, а `volume` и `attainmentPct` добавлены поверх неё.
  - Значит выбирать, «какой из двух источников становится страницей», не нужно: страница остаётся на `/api/v1/mtm/leaderboard` — один источник чисел, как и просит аудит, — а панель разбора открывается с клика по строке, потому что `NormalizedAgent` для этой строки получается из неё же, без второго запроса. Возможность, которую «никто не просил убирать», не теряется.
  - Что остаётся собственно работой: убрать холст с пузырьками и переключатель с MTM-страницы (`BubbleArena` не удалять — на нём стоит общая страница `leaderboard/page.tsx`), построить таблицу с KPI, периодом и сортировкой и повесить на строку открытие панели. Решения владельца это уже не требует: «не геймифицированная панель» записано в `mtm-routes-phase-1-ux-contract.md`.
  - **Сортировки на странице сейчас нет вовсе**, поэтому дефекта «Top 3 из отсортированного списка» тоже пока нет: `rankings.slice(0, 3)` берёт то, что вернул сервер. Он появится ровно в тот момент, когда сортировку добавят — это требование задачи, а не существующая поломка. Тест на это писать вместе с сортировкой.
  - **Сделано.** Пузырьковый холст и переключатель вида убраны со страницы MTM; `BubbleArena` не удалён — на нём стоит общая `/leaderboard`, а её этот аудит не открывал. Страница стала таблицей: агент, визиты, задачи, фото, баллы, с сортировкой по любому числовому столбцу и `aria-sort` на заголовке.
  - **Дефект «Top 3 из отсортированного списка» появился ровно в этот момент — и закрыт тем же коммитом.** Таблица рисуется из `sortedRankings`, а «Top 3» читает нетронутый `rankings`: срез отсортированного массива молча превратил бы «трёх лучших» в «трёх сверху того, что читатель только что отсортировал». Под заголовком стоит строка «по рейтингу, независимо от сортировки таблицы», чтобы это не выглядело расхождением. Тест держит оба конца.
  - Разбор KPI открывается кликом по строке и с клавиатуры (Enter/Пробел), у строки есть доступное имя. Attainment намеренно **не** пересчитывается на клиенте: веса задаются на организацию, и клиентская догадка напечатала бы не то число, которое вернёт API самой панели. Если arena-фид закрыт модулем или ролью, строка честно говорит, что разбор недоступен, вместо тихого бездействия.
- [x] **C14** Удаление в меню — P2 · S-plan · сверено с кодом 2026-09-08
  - Удаление агента и оповещения уехало в «⋯» тем же приёмом, что уже стоит на «Визитах». На карточке агента оно было на расстоянии промаха от «изменить», на оповещении — вплотную к «решено», а отменить его нельзя. Тест держит все три страницы разом.
- [x] **C15** Планшет 834 — P1 · S-plan · сверено с кодом 2026-09-08 · закрыто 2026-09-13 фактом на проде
  - Сделано: на `map/page.tsx` карта стала первым блоком на узких экранах — до этого два ряда фильтров и чипов занимали весь первый экран планшета, и менеджер открывал карту, не видя карты. Порядок переопределён только ниже `lg`, десктоп не тронут.
  - Первый пункт хвоста закрыт кодом и **проверен на проде 2026-09-10**: у корня диалога в живой странице стоит `items-stretch p-0 min-[900px]:items-center min-[900px]:p-4`. Планировщик берёт весь экран ниже **900 px**. Причина была в одной букве — диалог переключался на полный экран ниже `md`, то есть 768, и планшет из аудита (834) оказывался на стороне диалога с запасом в 66 px. У `Dialog` появилась точка `tablet`, и число 900 стоит теперь в трёх местах сразу: у диалога, у его высоты и у собственной высоты `route-builder`. Полноэкранная поверхность, содержимое которой всё ещё резервирует `calc(100dvh-2rem)`, вернула бы футер шага 3 под сгиб — это следующий пункт того же хвоста.
  - **Футер шага 3 на 1470×675 не обрезан — проверено на проде 2026-09-13.** Панель действий стоит на y=587–652, главная кнопка на 596–644 при высоте окна 675. Это ширина аудита для десктопа, а не 834: пункт хвоста про планшет этим не закрыт.
  - **Ловушка замера, чуть не ставшая ложным дефектом.** Первый замер показал футер на y=736–801, за нижним краем. Причина не в вёрстке: вкладка была фоновой (`visibilityState: hidden`, ноль кадров `requestAnimationFrame`), анимация появления страницы `MotionPage` застыла на 66-й мс из 300 с `transform: translateY(1.6px)`. Любой `transform` у предка превращает `position: fixed` диалога в позиционирование относительно этого предка (824 px), а не экрана. Когда анимация доведена до конца (`transform: none`), футер на месте. **Замеры фиксированных слоёв делать только после проверки, что у предков `transform: none`.**
  - **Ширина 834 измерена на проде 2026-09-13.** `resize_window` ширину не меняет, и встроить страницу во `iframe` тоже нельзя: наши же заголовки запрещают фрейм. Сработало всплывающее окно, которое страница открыла по настоящему нажатию: `innerWidth` 834, высота 695, вкладка видимая. Ничего не сохранялось: перехват `fetch` в том окне не записал ни одного не-GET запроса.
  - **Футер шага 3 — три дефекта на одном экране, исправлены кодом, ждут проверки на проде.** Замер при восьми клиентах:
    1. Корень `Dialog` рендерится на месте, внутри `space-y-3` страницы, и получал `margin-bottom: 12px`. Фиксированный `inset-0` с нижним отступом на столько же короче экрана: лист 683 px на экране 695, снизу видна страница. Исправлено `m-0` у корня. Правка общая для всех диалогов: нижний отступ фиксированному корню не нужен никогда.
    2. Скрытые подписи времени (`sr-only`, `position: absolute`) считали своим позиционированным предком диалог, а не прокручиваемую середину. Восемь подписей растянули `scrollHeight` диалога до 876, последняя кончалась ровно на 876. При входе на шаг 3 `scrollIntoView` прокручивал сам диалог на 89 px — `overflow: hidden` программную прокрутку не запрещает. Крестик закрытия уезжал на y −89…−53, степпер тоже за верхний край. Исправлено `relative` у середины.
    3. При малом числе клиентов планировщик держал высоту содержимого, и футер кончался на y=620, оставляя под собой 62 px пустого листа. Исправлено: обёртка и планировщик — `flex-1`.
  - Все три правки сначала применены инлайном на той же живой странице: корень 0–695, у диалога `scrollTop` 0 и `scrollHeight` = `clientHeight`, крестик 12–48, футер 609–694.
  - **Прокрутка к панели дня — дефект воспроизводился, исправлен кодом, ждёт проверки на проде.** В календаре после нажатия на 1 сентября страница стояла на месте: «Planlaşdır» на 664–708, единственный маршрут дня на 720–780 при высоте 695, а прокрутить можно было ещё 114 px. Теперь выбор дня вызывает `scrollIntoView({ block: "nearest" })` у панели, без анимации при `prefers-reduced-motion`. Повтор этого вызова на живой странице: страница сдвинулась на 96 px, панель 550–683, выбранный день остался виден на 314–358.
  - **Принято на проде 2026-09-13** ([#158](https://github.com/rashadoni/leaddrive-v2/pull/158), merge `922a6a9f`; выехал в деплое [34759255381](https://github.com/rashadoni/leaddrive-v2/actions/runs/34759255381) коммита `7eafbeef`, который его содержит — свой деплой отменён следующим мержем, `.deploy-sha` и `build-info` = `7eafbeef`). Окно 834×695, без единой инлайн-правки, запись заблокирована тем же способом, что в C8.
  - Шаг 3 с 2 и с 8 клиентами дал одинаковые цифры: у корня `m-0`, `margin-bottom: 0px`, корень 0–695, диалог 0–695 при `scrollTop` 0 и `scrollHeight` = `clientHeight` = 695, крестик 12–48, степпер 60–115, футер 609–694. До правки было 683 px, крестик на −89…−53 и футер на 620.
  - Календарь, 1 сентября: страница прокрутилась на 96,5 px, панель дня 550–683, «Planlaşdır» 567–611, маршрут дня 623–683, выбранный день виден на 314–358. До правки кнопка стояла на 664–708, маршрут на 720–780. Числа совпали с пробой, сделанной инлайном до PR.
  - Попутно: окно открылось на виде «Komanda həftəsi», для замера переключено на календарь и после проверки возвращено обратно.
- [x] **C16** Мелочи — P2 · S-plan · сверено с кодом 2026-09-08
  - Все три пункта решения владельца №3: отчёт «Stok dəyişikliyi» удалён из витрины, конфига и API вместе со своими строками и мёртвыми помощниками; пилюля «Plan» стала «Маршруты» (ключ тоже переименован — он вёл в `/mtm/routes`); часы на панели показывают часы и минуты, а тик стал раз в полминуты вместо секунды.

### Эпик D — Проверка · закрыто 2 из 3

- [ ] **D1** Мобильный E2E по adb — 
  - **проверить на телефоне:** склонения после #76 («1 точка», «3 точки», «11 точек» в истории GPS)
  - **проверить на телефоне:** азербайджанский инициал контакта на «i» (İ, не I) в карточке визита и на живой карте
  - ✓ **Проверено 2026-09-13:** ADV-DEMO Store 1 → «Plansız ziyarətə başla» → «Giriş» открывает «Bu nöqtənin koordinatları yoxdur» с «Rəhbərə bildir». Запроса местоположения и записи нет — ветка стоит в `performCheckIn` до них. Шероховатость: сначала спрашивается подтверждение входа, и только после него — что войти нельзя.
  - ✓ **Проверено 2026-09-14:** сеть выключена → «○ Oflayn» быстрее 5 с, возврат → «✓ Sinxronizasiya edilib» (см. B4).
  - ✓ **Проверено 2026-09-13:** после того как владелец добавил домен в кабинете CARTO — карта GPS-истории с тайлами, водяного знака нет (B16).
  - ✓ **Исправлено по находкам приёмки 2026-09-13, каждое проверено на телефоне:** статус-бар белым по белому на «Təqvim» и «Daha çox» ([rashadoni/leaddrive-mtm #22](https://github.com/rashadoni/leaddrive-mtm/pull/22), `899590a5`); имена клиентов на «Ziyarətlər» обрезались до «ADV-DE…» чипом координат ([#23](https://github.com/rashadoni/leaddrive-mtm/pull/23), `87ff428c`); центр синхронизации показывал общий заголовок и сырой `MTM_VISIT_CUSTOMER_NO_COORDINATES` без имени клиента ([#24](https://github.com/rashadoni/leaddrive-mtm/pull/24), `1768f0dc`).
  - Застрявший визит в центре синхронизации разобран по решению владельца: клиенту ADV-DEMO Store 1 записаны координаты улицы по OpenStreetMap (40.3545627, 49.8175522 — дом 13 в OSM не размечен, точность до улицы; запись в журнале аудита CUSTOMER_UPDATE); повтор вернул «вне зоны» (GPS попытки не у магазина, принудительный вход агенту недоступен); операция удалена с телефона — «Ziddiyyət yoxdur». Пункт D1 про точку без координат теперь проверяется на Store 2 или Store 3.
  - ✓ **Найдено владельцем на телефоне и исправлено 2026-09-13:** чип синхронизации под датой на «Сегодня» при конфликте был белым текстом на `#fff7ed` (контраст 1,06:1). [rashadoni/leaddrive-mtm #19](https://github.com/rashadoni/leaddrive-mtm/pull/19), merge `f39f4526`: цвета конфликта применяются после `inverse` — подпись `#9a3412` (6,88:1), значок `#c2410c` (4,88:1). На телефоне в `v3.1.0-build67` читается.
- [x] **D2** Веб-прогоны — начаты 2026-09-10 · S-plan · закрыто 2026-09-14 решением владельца на ручных проверках
  - Первый проход сделан **в браузере владельца** через расширение Chrome, на `/mtm/routes`, ширина 1470×675 — та же, что в аудите. Ниже только то, что видно на экране или прочитано из живой страницы; всё, что «по коду выглядит сделанным», сюда не пишется.
  - **C5 измерена, не оценена на глаз.** Первая строка данных начиналась на `y=312`, то есть 46 % окна уходило на шапки: приложение 56 px, полоса модуля 57, заголовок страницы 63, шапка карточки ~70. AI-строки на этой странице уже нет — проверено в дереве страницы. Заголовок сведён в одну строку тем же проходом.
  - **C6 подтверждена фактом.** На странице 35 ячеек с `data-past-day`, и прошедшие дни видимо приглушены — и число, и «+ Planlaşdır». Выходные при этом **не** затенены, и это правильно: у тенанта `enforceWorkCalendarForRoutes: false`, а запись C6 ровно это и обещает — «пока тенант его не включил, серая суббота была бы правилом, которого у продукта нет». Совпадение экрана с записью — тот случай, когда галочка заслужена.
  - **C7: фильтр по команде на этом тенанте проверить нечем.** Все 16 агентов приходят с `teamId: null`, команд ноль, поэтому `teams.length > 1` ложно иконтрол намеренно не рисуется. Это не «работает» и не «сломано»: данных для проверки нет. Закрывать C7 галочкой до появления хотя бы двух команд нельзя.
  - **C15 в этот проход не проверена:** `resize_window` до 834×686 отчитался успехом, но `window.innerWidth` остался 1470. Ширину надо ставить руками или другим окном.
  - **Проход 2026-09-13: ширина 834 получена, C8 и C15 закрыты на проде.** Как ставить ширину: `iframe` не годится, наши заголовки запрещают фрейм. `window.open` из скрипта блокирует попап-блокер. Работает кнопка, которую скрипт добавляет на страницу, плюс настоящий клик по ней через `computer`: попап с `width=834`, доступный из открывшей вкладки как same-origin окно. Имя окна при каждом открытии новое, иначе браузер переиспользует прежнее окно вместе с его размером (второй раз вышло 1470).
  - **Две ловушки замера, обе записаны, чтобы не стать ложным дефектом.**
    1. В фоновой вкладке таймеры и `requestAnimationFrame` стоят. Анимация `MotionPage` застывает с `transform`, и `position: fixed` начинает считаться от обёртки — подробности в C15.
    2. Долгий скрипт в фоновой вкладке с её же `setTimeout` упирается в 45-секундный таймаут CDP. Ждать нужно таймерами видимого окна (`popup.setTimeout`).
  - **Проверки без записи — страховкой, а не аккуратностью.** Перед любым нажатием «сохранить/опубликовать» в окне подменялся `fetch`: не-GET запросы отклонялись, кроме `meeting-availability`, который ничего не пишет. Отклонённых за оба прохода — ноль.
  - **Вторая половина D2 сделана 2026-09-13: в AZ нет «M0x» и английских дней — на обеих ширинах.** Пройдены все 21 страница `/mtm/*` (детальные — на первой записи своего списка: контакт, клиент, промо-акция, задача) и четыре вида «Маршрутов» (неделя, календарь, список, матрица), каждая при 834×695 и 1470×695, `lang="az"`. Искалось в видимом тексте и в `aria-label`/`title`/`placeholder`: `M0\d`, английские дни (полные и сокращения) и, сверх требования, английские месяцы. **Находок ноль.**
  - Поиск с границами по буквам Unicode, а не `\b`: `\b` считает «ı» не буквой и снова нашёл бы «Sat» в «Satış» — ложную тревогу первого прохода. Сканер перед проходом проверен на подложенной строке «Satış · 2026 M09 · Mon 14 · 3 Sep»: нашёл `M09`, `Mon`, `Sep` и не тронул «Satış». Проверялось состояние страницы после загрузки; раскрывающиеся выборы дат не открывались. Вид «Маршрутов» после прохода возвращён на «Komanda həftəsi».
  - **Сценарии RUX (`record-mtm-routes-guide.yml`, `swissmed-mtm-browser-evidence.yml`) не запускались — точный блокер.** В `rashadoni/leaddrive-v2` нет секретов, без которых оба падают на шаге проверки: `MTM_EVIDENCE_ADMIN_EMAIL`/`_PASSWORD`, `MTM_EVIDENCE_AGENT_EMAIL`/`_PASSWORD`, а первому ещё `GEMINI_API_KEY`; при переезде репозитория они не перенесены. Это пароли QA-учёток — завести их может только владелец. Даже с секретами запуск не безобиден: сценарии пишут в боевую базу (сброс `[QA-SWISSMED]`-фикстур по SSH, черновик маршрута с удалением, назначение из планировщика) и держат группу `production-deploy`. Ширины 834 среди их вариантов нет (`tablet` — 1024, `tablet-landscape` — 1180), так что для C15 они ничего бы не добавили.
  - Путь, который проходит `routes-guide`, на проде пройден вручную на обеих ширинах: сотрудник, два и восемь клиентов, шаг 3, подтверждение публикации. Не сделано ровно последнее нажатие «сохранить» — оно пишет в базу.
  - **Закрыто 2026-09-14 решением владельца: автоматические сценарии RUX не нужны.** Всё, ради чего заведён D2, сделано на проде руками и записано выше: ширины 1470 и 834, проверка DOM на «M0x» и английские дни на всех 21 странице `/mtm/*`, планировщик и календарь на 834. Автоматический прогон добавил бы только повторяемость, а стоил бы паролей QA-учёток в секретах (их заводит только владелец) и записи `[QA-SWISSMED]`-фикстур в боевую базу. `GEMINI_API_KEY` нужен лишь для озвучки видео-гайда — к проверкам D2 отношения не имеет, заводить не нужно.
  - **Третий проход 2026-09-10: закрытые задачи проверены на экране, а не по галочке.** Это то, ради чего D2 и заведён — «доставка ≠ приёмка».
  - **C11 подтверждена арифметикой.** На «Задачах» стоит «Cəmi: 1531 · Gözləyir: 165 · İcradadır: 123 · Tamamlanıb: 1243». 165 + 123 + 1243 = 1531 — счётчики статусов и «Всего» приходят из одного отфильтрованного набора. До правки рядом стояли серверное «Всего» и статусы, посчитанные по строкам текущей страницы; сходиться они не были обязаны. Сроков «00:00» на странице нет — вторая половина C11.
  - **C12 подтверждена на журнале активности:** ни имени таблицы (`mtm_*`), ни шестизначного обрывка идентификатора, ни сырых значений перечислений, ни дат вида «2026 M09».
  - **A5/T10 подтверждены на карточках агентов:** ни `AGENT`, ни `ADMIN`, ни `ACTIVE` в тексте страницы. C14 согласуется: кнопок удаления на верхнем уровне нет.
  - **Ложная тревога, которую стоит записать.** Автоматический поиск английских дней недели нашёл «Sat» — это оказалось азербайджанское «Satış» в боковом меню. Грубый греп по трёхбуквенным сокращениям на азербайджанской странице даёт ложные срабатывания; проверять надо текстовый узел, а не страницу целиком.
- [x] **D3** Документы — S-plan · 2026-09-10
  - Роадмап `mtm-routes-ux-roadmap-2026-08-23.md` получил секцию «Phase 10 — Field UX audit 2026-09-05»: строка RUX-1001…RUX-1048 на каждую задачу этого плана, со ссылкой сюда. Таблица собрана **из раздела 0.1**, а не набрана руками, и это записано в самой секции: два места с состоянием разъезжаются, поэтому там прямо сказано, что при расхождении право за 0.1.
  - В статусах роадмапа появилось различие, которого остальным фазам не нужно: **MERGED** — код в `main` и в собранном APK, но на устройстве его никто не видел. В этом плане это не «сделано»: аудит и существует потому, что одиннадцать его находок тесты не видят.
  - Вторая половина D3 оказалась уже выполненной: контракт ошибок чек-ина (A6) стоит в `mtm-mobile-update-spec-2026-07-11.md`, раздел 2.4, с датой 2026-09-05 и таблицей кодов. Проверено, а не переписано заново.

## 1. Эпик A — данные и API (`leaddrive-v2`), блокирует B и C

### A1. Координаты «нет данных» вместо 0,0 — P0
- **Где:** модели точек в `prisma/schema.prisma` (Customer/Organization lat/lng), `src/app/api/v1/mtm/customers/*`, `src/app/api/v1/mtm/mobile/routes/targets/route.ts`, `src/app/api/v1/mtm/mobile/sync/pull/route.ts`, веб `src/components/mtm/route-map.tsx`.
- **Что:** отсутствующие координаты хранятся и отдаются как `null`, никогда как `0`. Клиенты не считают расстояние при `null`.
- **Приёмка:** у ADV-DEMO Store 1–4 в ответах API `lat/lng` равны `null`; веб-карта маршрута показывает пустое состояние «у точек нет координат» вместо маркера в океане; приложение не показывает «6745.7 km».

### A2. Один источник «доступных точек» для агента — P0
- **Где:** `src/app/api/v1/mtm/mobile/routes/targets/route.ts` (планировщик), выдача клиентов для «Визитов» и «Клиентов» (`mobile/sync/pull`, `mtm/customers`), каталог веб-планировщика в `route-builder.tsx`.
- **Что:** одна функция eligibility в `src/lib/mtm/` с кодом причины (`assignment`, `territory`, `policy`, `weekend`, `date`), которую используют все три экрана приложения и веб.
- **Приёмка:** для Anar Mammadov на 7 сентября планировщик, «Визиты» и «Клиенты» показывают один и тот же набор точек (сегодня: 0 против 3 против 3). Если набор пуст, все три экрана называют одну причину. Юнит-тест на функцию.

### A3. Незавершённые маршруты — P0
- **Где:** новый крон `src/app/api/cron/mtm-route-day-close` по образцу `mtm-auto-checkout`; статусы в `src/components/mtm/route-types.ts`; список и карточка на вебе; `WeekScreen.tsx` в приложении.
- **Что:** в конце дня по таймзоне организации маршруты прошедших дней со статусом `IN_PROGRESS` или `PLANNED` переводятся в `INCOMPLETE` («Yarımçıq» / «Не завершён») с сохранением прогресса; фильтр и чип на вебе, статус на карточке дня в приложении.
- **Приёмка:** «Davam edir» содержит только сегодняшние маршруты; 14 августовских переведены; крон добавлен в пост-деплойные проверки `deploy.yml` по правилам про `cron-trigger.sh`.

### A4. Локализованные оповещения — P1
- **Где:** генераторы в `src/lib/mtm/*` (auto-checkout, route deviation, geofence), рендер `src/app/(dashboard)/mtm/alerts/page.tsx`, профиль приложения.
- **Что:** хранить `type` и `params`, текст собирать на клиенте из i18n; легаси-строки отдавать как есть с признаком `legacy`.
- **Приёмка:** новые оповещения на AZ/RU/EN; «Активные» в приложении — только нерешённые за последние N дней, июньские не показываются как активные.

### A5. Словарь статусов — P1
- **Где:** новый `src/lib/mtm/status-labels.ts` + ключи в `messages/*.json`; в приложении `src/lib/status-labels.ts` + ключи в `locales/*.json`.
- **Что:** статусы визитов (`Checked out`, `Successful`), организаций (`Active`), роли (`AGENT`), маршрутов, типов дня (`WEEKEND`, `WORKDAY`, `HOLIDAY`; см. `kind` в `src/app/api/v1/mtm/mobile/week/route.ts`).
- **Приёмка:** grep по UI-компонентам не находит сырых значений; тест паритета ключей словаря.

### A6. Контракт ошибок чек-ина — P0
- **Где:** `src/app/api/v1/mtm/visits/route.ts`, `src/app/api/v1/mtm/mobile/sync/push/route.ts`, спека `docs/mtm-mobile-update-spec-2026-07-11.md`.
- **Что:** явные коды `TOO_FAR{distance,max}`, `NO_COORDINATES` (чек-ин отклоняется всегда, решение владельца 2), `ACTIVE_VISIT`, `ROUTE_MISMATCH`, `CUSTOMER_MISSING` и их документирование. Приложение обрабатывает каждый (B1).

### A7. Перерыв в рабочем дне — P1
- **Где:** `src/app/api/v1/mtm/mobile/workday/*` (сейчас есть старт, завершение и пауза со стороны HRM), модель рабочего дня, `mtm/map` и `mtm/agents` на вебе (статусы «в сети / перерыв»).
- **Что:** агент ставит паузу и возобновляет из приложения; сервер хранит отрезки, GPS-точки на паузе не принимаются как рабочие; «Завершить день» остаётся окончательным и однократным.
- **Приёмка:** отрезки видны в карточке агента на вебе и в «Моя GPS-история»; тест на запрет повторного старта после завершения.

## 2. Эпик B — приложение (`leaddrive-mtm/MTMobileApp`)

### B1. Внеплановый визит: любой исход виден — P0
- **Где:** `src/screens/visit/VisitScreen.tsx`, `performCheckIn` (около строк 530–600). При `null`-координатах `haversineDistance` даёт `NaN`, проверка `> GEOFENCE_DEFAULT` не срабатывает, запрос уходит на сервер, ответ об ошибке не показывается.
- **Что:** ветка «у точки нет координат» блокирует чек-ин с сообщением и кнопкой «Сообщить руководителю» (решение владельца 2), явный диалог на каждый код из A6, кнопка «Повторить» для сетевых ошибок.
- **Приёмка:** jest на четыре ветки (успех, слишком далеко, нет координат, ошибка сервера); на телефоне выбор ADV-DEMO Store 1 → «Зайти» заканчивается сообщением про координаты, а не молчанием.

### B2. Расстояние до точки — P0
- **Где:** `src/screens/route/RouteScreen.tsx` (`formatDistance`, строка ~316), список «Доступные клиенты» на «Визитах».
- **Что:** при `null` показывать «координаты не заданы», такие точки в конец списка; никаких вычислений от 0,0.

### B3. Состояние «день завершён» — P0
- **Где:** `src/screens/today/TodayScreen.tsx` (`workdayOpen`, `workdayEnding`, строки ~120 и ~483), стор рабочего дня, ключи `todayV2.*` в `src/i18n/mobile-resources.ts`.
- **Что:** после подтверждения карточка «День завершён в 18:24» без возможности начать заново в тот же день (решение владельца 4); `FINISH_PENDING` показывать как «Завершаем…». Добавить «Перерыв» и «Продолжить» с остановкой и возобновлением GPS (сервер: A7); во время перерыва карточка «Перерыв с 13:05», чек-ины заблокированы с объяснением.
- **Приёмка:** скриншоты четырёх состояний (не начат, идёт, перерыв, завершён), тест редьюсера, GPS-точки не пишутся во время перерыва.

### B4. Офлайн — P0
- **Где:** `src/runtime/AndroidApp.tsx` (NetInfo уже подключён), `src/components/SyncStatusChip.tsx`, баннеры в `WeekScreen.tsx` и контактах.
- **Что:** глобальный `isOnline` в сторе; чип «Офлайн · N ожидают» (ключи `syncCenter.offline*` уже есть); центр синхронизации показывает офлайн; «Обновить» в офлайне объясняет, почему не обновило.
- **Приёмка:** авиарежим → чип меняется не позже чем через 5 секунд; возврат сети → «Синхронизировано».

### B5. Карточка дня в календаре — P0
- **Где:** `src/screens/week/WeekScreen.tsx` (`DayMetric` ~841, `noAgenda` ~161), сервер `mobile/week/route.ts` (`kind`).
- **Что:** чип статуса маршрута; «На этот день планов нет» только при нуле точек и задач; карточка открывает экран дня со списком точек и кнопкой «Открыть маршрут» для сегодняшнего дня; `kind` через словарь A5.
- **Приёмка:** на карточке 1 сентября: «3 точки · Запланирован», карточка открывается.

### B6. Компоновка календаря — P1
- **Где:** `WeekScreen.tsx`.
- **Что:** лента семи дней (число и точка, если есть план) и одна раскрытая карточка выбранного дня; открывать на сегодняшнем дне; кнопка «Сегодня» выбирает сегодня; сводка недели в одну строку.
- **Приёмка:** неделя и выбранный день помещаются в один экран 2316 px.

### B7. «Сегодня» в один экран — P1
- **Где:** `TodayScreen.tsx`.
- **Что:** убрать «Быстрые действия» (дублируют вкладки); «Коротко за сегодня» перенести в сводку календаря; иллюстрацию «Следующий шаг» уменьшить; порядок: состояние дня → следующий шаг → следующая точка маршрута.
- **Приёмка:** без маршрута экран не прокручивается; с маршрутом следующая точка видна над сгибом.

### B8. Планировщик своего маршрута — P1
- **Где:** `src/screens/planning/PlanningWorkspaceCore.android.tsx` (`SELF_PLANNER_COPY` строки 129–160, подсказка ~797–1070, шаг 1 ~1222+), `src/screens/route/RouteSelfPlanningWorkspace.android.tsx`, `src/services/bootstrap.ts` (`policies.canPlanOwnRoutes`).
- **Что:**
  1. элементы управления первыми; «Как это работает?» и «Что сделать сейчас» показываются только при первом входе, дальше за «?»;
  2. заголовок шага 1 в self-режиме: «Ваш маршрут: выберите дату», без «выберите сотрудника»;
  3. выбор даты календарной сеткой с пометкой выходных и праздников; по умолчанию ближайший рабочий день;
  4. дата на шаге 2 показывается один раз;
  5. пустое состояние с настоящей причиной из A2 и без совета «в веб-версии»;
  6. вход «Составить мой маршрут» скрыт, если политика или eligibility не дают кандидатов, с объяснением в карточке.
- **Приёмка:** сценарий «понедельник, две точки, черновик, публикация» проходит на телефоне под Anar Mammadov.

### B9. Экран «Маршрут» без маршрута — P1
- **Где:** `RouteScreen.tsx` (степпер, `Обновить маршрут` ~1400, карточка «Нужен визит вне маршрута?» ~2002).
- **Что:** степпер только при наличии маршрута; один способ обновления (pull-to-refresh или кнопка); «Внеплановый визит» — одна кнопка в шапке вместо взаимных карточек с «Визитами».

### B10. Задачи — P1
- **Где:** `src/screens/tasks/TasksScreen.tsx` (сегменты ~643), `src/screens/tasks/TaskDetailScreen.tsx`.
- **Что:** фильтры без нумерации «1 · 2 · 3»; приоритет как факт («Высокий»), не совет; заголовок карточки — название задачи; наставления в разделах убрать, для завершённых задач показывать только результат.

### B11. «Ещё» и профиль — P2
- **Где:** `src/screens/more/MoreScreen.tsx`, `src/screens/profile/ProfileScreen.tsx`.
- **Что:** убрать четыре вступительных текста; убрать «Показатели за сегодня» (дубль главной); «Моя GPS-история» только в «Ещё»; чип синхронизации только в шапке «Сегодня».

### B12. Центр синхронизации словами пользователя — P2
- **Где:** `SyncStatusChip.tsx`, ключи `syncCenter.pipeline*`.
- **Что:** «Контуры синхронизации» → «Что синхронизируется»; пилотные и выключенные контуры не показывать; дата без секунд, в общем формате приложения.

### B13. Верхний регистр в азербайджанском — P1
- **Где:** 94 места с `textTransform: "uppercase"` или `toUpperCase()`: `SyncStatusChip.tsx`, `GpsHistoryScreen.tsx`, `TaskDetailScreen.tsx`, `DoctorAssessmentModal.tsx`, `ContactEditModal.tsx`, `DashboardScreen.android.tsx`, экраны менеджера и другие.
- **Что:** helper `upper(text, lang)` на `toLocaleUpperCase("az")` либо отказ от капса в eyebrow-подписях.
- **Приёмка:** «KÖMƏKÇİ», «NÖVBƏTİ ADDIM», «HƏFTƏNİN YEKUNU», «SÜRƏTLİ KEÇİD».

### B14. Русские плюралы — P1
- **Где:** `src/i18n/locales/ru.json` (`week.stopsTemplate`, `gpsHistory.statPoints`, `visit.visitsCount`, `task.totalTemplate`, `organizations.contactsTemplate` и другие с `{{n}}`), i18next v4 формы `_one/_few/_many`.
- **Приёмка:** «1 точка / 3 точки / 22 точек».

### B15. Сырые значения в интерфейсе — P1
- **Где:** карточка организации (`Магазин · Active`), история визитов (`Checked out · Successful`), профиль (`AGENT`), календарь (`WEEKEND`). Через словарь A5.

### B16. Карта GPS без водяного знака — P1
- **Где:** `src/screens/maps/carto-tiles.ts` (тайлы `basemaps.cartocdn.com` без ключа).
- **Что:** ключ CARTO из bootstrap/config либо переход на тайлы OSM с корректной атрибуцией.

### B17. Диалог завершения дня — P2
- **Где:** `TodayScreen.tsx` строки ~212–216 (`Alert.alert`).
- **Что:** `src/components/ConfirmSheet.tsx`; «Продолжить работу» первым, «Завершить день» вторым и красным.

### B18. Доступность — P2
- **Где:** `WeekScreen.tsx:1049` и аналогичные `accessibilityLabel` из массива с иконкой (даёт «, Начать день»), `src/navigation/AppNavigatorAndroidV2.tsx` (`tabLabel` 10 px).
- **Что:** описание без глифа; подпись вкладок 12 px.

### B19. Планшет от 600 dp — P1
- **Где:** `src/theme/layoutBreakpoints.ts`, экраны Calendar, Tasks, Base.
- **Что:** две панели (список слева, деталь справа); шапка в одну строку в ландшафте.
- **Приёмка:** скриншоты 824 dp в ландшафте на телефоне и планшет в портрете, когда появится устройство или рабочий эмулятор.

### B20. Экран входа — P1
- **Где:** `src/screens/auth/LoginScreen.tsx`, `src/screens/server/ServerScreen.tsx`.
- **Что:** заставка не выше четверти экрана; поля и кнопка над сгибом; фокус в первом поле.

### B21. История визитов на «Визитах» открывается — P2
- **Где:** `VisitScreen.tsx` (список истории), по образцу карточки организации (`RouteOrganizationDetailScreen.android.tsx`).

### B22. Сборки — P2
- x86_64 только для эмуляторов: ветка `claude/appetize-x86_64` и релиз `v3.0.4-build197` в прод не тянуть; при необходимости отдельный CI-флаг `EMULATOR_BUILD`.

## 3. Эпик C — веб (`leaddrive-v2`, `/mtm/*`)

### C1. Имя клиента из текстов продукта — P0
- **Где:** `messages/en.json` строки 13730, 13764, 14016, 14022, 14070, 14677, 14689, 14704, 14818 и те же ключи в `ru.json`, `az.json`; `src/content/help/mtm-analytics/{az,en,ru}.tsx`; `src/lib/mtm-settings.ts:89`.
- **Что:** нейтральные формулировки («утверждённые документы тенанта»).
- **Приёмка:** `grep -ri swissmed src messages` находит только QA-фикстуры тестов.

### C2. Даты только через хелпер — P0
- **Где:** 69 прямых вызовов в 12 файлах: `src/app/(dashboard)/mtm/page.tsx:30,219`, `route-week-plan.tsx`, `live-map.tsx`, `operational-week-home.tsx`, `explainable-kpi-dashboard.tsx`, `organization-explorer.tsx`, `pharmacy-promotion-workspace.tsx`, `pharmacy-promotion-detail.tsx`, `pharmacy-promotion-agent-capture.tsx`, `task-edit-form.tsx`, `contact-dictionary-assignment-panel.tsx`, `settings/contact-dictionary-settings.tsx`, `route-map.tsx`; плюс `route-calendar.tsx:218,251` (`toLocaleDateString` в aria-label).
- **Что:** `formatDate`/`formatDateTime` из `src/lib/format-date.ts`; правило ESLint `no-restricted-syntax` на `toLocaleDateString`/`Intl.DateTimeFormat` в `src/components/mtm` и `src/app/(dashboard)/mtm`; дни недели в командной неделе через хелпер.
- **Приёмка:** в AZ-локали в браузере нет «M0x» и «Mon/Tue/Wed».

### C3. Язык страницы из локали — P1
- **Где:** `src/app/layout.tsx:84` (`<html lang="en">`).

### C4. «Не завершён» на вебе — P0 (вместе с A3)
- Чип, фильтр, карточка маршрута с датой и причиной.

### C5. Первый экран — P1
- **Где:** `src/components/ai/content-search-bar.tsx` (AI-строка), `src/components/mtm/mtm-module-navigation.tsx`, `src/components/mtm/mtm-workflow-guide.tsx` (карточки «3 addım» на visits, operations, customers, contacts, promotions), заголовки страниц.
- **Что:** AI-строка сворачивается до иконки внутри модуля и запоминает состояние; гайд показывается только при первом входе с «больше не показывать»; заголовок страницы в одну строку.
- **Приёмка:** календарь, карта и таблицы начинаются в первом экране при 1470×657 и 834×686.

### C6. Ячейки календаря — P1
- **Где:** `src/components/mtm/route-calendar.tsx` (кнопка строка 251).
- **Что:** «+» виден по hover/focus (и всегда на touch); прошедшие дни приглушены; выходные и праздники затенены с причиной (RUX-404).

### C7. Командная неделя — P1
- **Где:** `src/components/mtm/route-week-plan.tsx`.
- **Что:** QA-аккаунты (`[QA-`) скрыты фильтром; поиск и фильтр команды (RUX-602); тень прокрутки у обрезанных колонок; дни недели через хелпер.

### C8. Планировщик — P1
- **Где:** `src/components/mtm/route-builder.tsx`.
- **Что:** шаг 1 — кнопка «Növbəti: Görüş yerləri» и инлайн-валидация; шаг 2 — одна главная кнопка; кегль карточек не меньше 12; шаг 3 — публикация без времён требует явного подтверждения; фиксированная высота модала и scroll-lock страницы; подтверждение отмены при выбранных точках; ключ автосохранения «дата + агент», побеждает последняя версия (проверить случай, когда восстановился черновик 09:43 вместо 15:23).

### C9. Индикатор вида — P2
- **Где:** `src/app/(dashboard)/mtm/routes/page.tsx` (подпись выпадающей кнопки). RUX-106.

### C10. Карточка маршрута — P1
- **Где:** `routes/page.tsx` (`AdvisorRecordWidget`), `route-map.tsx`.
- **Что:** убрать блок «Advisor riski» (RUX-604); пустое состояние карты при `null`-координатах (A1).

### C11. Счётчики задач — P1
- **Где:** `src/app/(dashboard)/mtm/tasks/page.tsx`.
- **Что:** «Cəmi» и постраничные счётчики из одного источника с понятной подписью; срок без времени для задач с датой без времени.

### C12. Служебная лексика — P1
- **Где:** `explainable-kpi-dashboard.tsx` (код формулы, «источник не менялся»), `activity/page.tsx` (фрагмент ID), `alerts/page.tsx` (A4).

### C13. Рейтинг — P2
- **Где:** `src/app/(dashboard)/mtm/leaderboard/page.tsx`.
- **Что:** пузырьковую карту убрать совсем (решение владельца 3); рейтинг — таблица с KPI, периодом и сортировкой.

### C14. Удаление в меню — P2
- **Где:** `src/app/(dashboard)/mtm/agents/page.tsx`, `alerts/page.tsx`. Иконка удаления убирается в меню «⋯».

### C15. Планшет 834 — P1
- **Где:** `map/page.tsx` (карта первым блоком), `route-builder.tsx` (полноэкранный лист ниже 900 px, футер шага 3 без обрезки), `routes/page.tsx` (после выбора дня прокрутка к панели действия).

### C16. Мелочи — P2
- «Plan» → «Marşrutlar» в пилюлях модуля; часы без секунд на панели; отчёт «Stok dəyişikliyi» удалить из `/mtm/reports` и `src/lib/mtm/report-config.tsx` (решение владельца 3).

## 4. Эпик D — проверка и свидетельства

### D1. Мобильный E2E по adb
- Скрипт `MTMobileApp/scripts/e2e-adb.sh` (снимок экрана с уменьшением, дамп `uiautomator`, tap/swipe/text/key) и чек-лист сценариев из отчёта; скриншоты RU/AZ экранов «Сегодня», «Календарь», планировщик шаг 1, «Задачи» прикладываются к PR.

### D2. Веб-прогоны
- Существующие guide-workflows RUX-* на 1470 и 834; дополнительная проверка DOM на отсутствие «M0x» и английских дней в AZ.

### D3. Документы
- Добавить в `docs/mtm-routes-ux-roadmap-2026-08-23.md` строки RUX-10xx для задач этого плана со ссылкой сюда; обновить `docs/mtm-mobile-update-spec-2026-07-11.md` контрактом ошибок чек-ина (A6).

## 5. Порядок и оценка (один разработчик, рабочие дни)

| Спринт | Задачи | Оценка |
|---|---|---|
| 1. Дефекты | A1, A6, B1, B2, B3, B4, B13, B14, B16, C1, C2, C3, A5 + B15 | 7–9 |
| 2. Первый экран | B7, B6, B5 + A3 + C4, B8 + A2, B9, B10, B11, B20, C5, C6, C8 | 8–10 |
| 3. Дубли, словарь, планшет | B12, B17, B18, B19, B21, C7, C9–C16, A4, A7, D1–D3 | 6–8 |

Внутри спринта серверные задачи (A*) идут первыми: без A1 и A2 мобильные B1, B2, B5, B8 нельзя принять.

## 6. Решения владельца (приняты 2026-09-05)

1. **Агент может планировать себе маршруты.** B8 выполняется полностью, A2 обязателен: список кандидатов у агента должен совпадать с «Визитами» и «Клиентами».
2. **Чек-ин у точки без координат запрещён.** B1: приложение объясняет «у точки нет координат» и предлагает сообщить руководителю; A6: код `NO_COORDINATES`. Никакого «отметиться всё равно».
3. **Рейтинг-«пузырьки» и отчёт «Stok dəyişikliyi» убрать.** C13: рейтинг становится таблицей без визуализации; C16: отчёт удаляется из `/mtm/reports` вместе с его конфигурацией.
4. **Вместо повторного старта — пауза.** «Завершить день» остаётся окончательным и один раз в день; добавляется «Перерыв» с возобновлением (A7, B3).

## 7. Как начать новую сессию

- Веб: `cd /home/rashad/projects/leaddrive-v2`, ветка от актуального `main` (например `claude/field-ux-sprint1`), прочитать `CLAUDE.md`, этот план и отчёт аудита.
- Приложение: `gh repo clone rashadrahimov/leaddrive-mtm` на Mac (или в scratchpad на Contabo только для чтения; сборки на Contabo запрещены). Тесты `cd MTMobileApp && npx jest`. Сборка через CI пушем в `claude/**` (prerelease APK в Releases), установка на телефон `adb install -r`, проверка на телефоне по adb-туннелю (память `android-phone-via-adb-ssh-tunnel`).
- Не трогать: ветку `claude/appetize-x86_64` (только для эмуляторов) и демо-данные QA-аккаунтов без согласования.
