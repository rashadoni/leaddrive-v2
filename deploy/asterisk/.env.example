# Заполнять ТОЛЬКО на сервере. Файл .env в git не коммитится.
# Пароли не пересылать в переписке и не хранить в репозитории.

# Пароль SIP-транка. Взять в 3CX: Voice & Chat -> 994995111103 ->
# Authentication password -> кнопка «показать».
TRUNK_PASSWORD=

# Новый пароль для регистрации софтфона LeadDrive (экстеншен 1001).
# Придумать длинный случайный, например: openssl rand -base64 24
WEBRTC_PASSWORD=

# Публичный IP сервера — тот, что уже в whitelist у оператора.
EXTERNAL_IP=185.161.225.131
