/**
 * Workforce-native browser transport. The compatibility path and this path
 * intentionally export the same authenticated handler and canonical state
 * machine; they must never evolve into two implementations.
 */
export { POST } from "@/app/api/v1/mtm/week/workday/route"
