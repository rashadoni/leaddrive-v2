# Workforce does not ship device key material, raw QR values or raw location
# in telemetry. Add narrowly justified R8 keep rules only when a real release
# dependency demonstrates a need; broad reflection keep rules hide regressions.
