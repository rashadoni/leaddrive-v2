name: Set PBX voice speed (retired)

# Retired with the OpenAI voice runtime. Keeping a failing tombstone makes old
# runbooks safe: dispatch cannot mutate any provider configuration.
on:
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-24.04
    steps:
      - name: Refuse the retired mutation path
        run: |
          echo "::error::The legacy PBX voice-speed workflow is retired."
          exit 1
